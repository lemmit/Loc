# Loom — the global implementation plan

*Created 2026-07-13 from a full re-classification of the design corpus: all 118 `docs/proposals/*`, all 71 `docs/plans/*`, the DEBT backlog, the parity registers, the audit findings, and the 2026-07 architecture weak-spot review. This document — plus the track files beside it — **supersedes** `docs/old/proposals/global-implementation-plan.md`, the `docs/old/proposals/README.md` status table, and `docs/old/plans/debt-prioritized-backlog.md` as the single source of ordering and open work. The archived corpus under [`../old/`](../old/) remains the design record (grammar sketches, semantics, rationale) — missions link into it; nothing was deleted.*

*Last refreshed: **2026-08-05** — a philosophy + drift audit over ~110 commits since 07-30 and all 14 open PRs (three parallel code-verified sweeps: main-merge conformance, open-PR coherence, tracker drift). Verdict on the code: the window conforms unusually well — mutation proofs stated and spot-run real, waivers ratchet with no stale slack, layering held, gaps named rather than silent. Three code-side findings: (1) **f4df492/#2419 is an empty commit on `main`** whose message narrates a fix that had already landed in #2413 — two agents fixed the same defect because neither claimed it first (the third orbit of `vanilla-liveview-read.ddd`; the claim-first rule exists for exactly this); (2) the walker **`Table` pager chrome was a silent i18n gap** both chrome inventories structurally missed (the text lives in target seams, not `_walker/primitives/`), widened by #2374's auto-paging — now claimed by open PR #2430 and recorded under M-T1.11; (3) the **.NET masked-audit-snapshot divergence** was waived only in `docs/auth.md` prose while T3's text claimed the opposite — promoted into M-T3.9 as a tracked open item. Tracker flips this pass: **M-T4.5 re-flipped to `done`** — its #2228 `done` had been **silently reverted by #2283's stale-base doc merge** (a status flip is as exposed to the stale-base hazard as code; verify `done`s against file history, not memory); M-T1.3 `open → partial` (phase ledger synced to its current scope doc — group-by #2365/#2417, `Chart` on all 8 tsx packs, Defect A/B closed, Vue #2366 + Svelte #2369 ports landed, Angular #2376 in-flight); M-T1.11 header corrected (the epic is well past "foundation"; chrome series recorded); M-T3.7 (b) `done`/(c) in-flight #2442; M-T4.8 recorded its shipped in-system typed api calls (#2291/#2318/#2326/#2341/#2342 — the body had never picked up its own headline); T4 header's stale "channels not CI-wired" corrected; M-T6.x renumbered **M-T6.26** `partial` (create-path done #2377, update halves #2392, residue in-flight #2440); M-T9.14 `open → partial` (Flutter has runtime gates now); M-T9.20 `open → partial` + widened to own the frontend-expression workstream (#2346/#2348/#2353/#2355, open #2439); **M-T9.25/M-T9.26 minted** (IDs were claimed by open PRs #2340/#2396 with no tracker entry); M-T8.12/M-T8.13 bodies pruned per the done-body rule; §Sequencing + §Priority shortlist rewritten (the 07-14 shortlist had fully shipped). Open action for a follow-up pass: coverage.md's quality-audit R-rows (R3–R5, R10–R12) still say "needs missions" — mint them before the note rots into a permanent placeholder. Open-PR attention list: #2340 (dirty, RS-id collision with main, partially superseded by #2429 — needs rebase + renumber + re-verify; blocks #2396), #2437 (dirty vs #2435's HEEx seam + retro-§ race with #2415), #2443 (sign-off must move `docs/auth.md` §"exactly equivalent" with the code).*

*Numeric-types audit (**2026-08-23**): a full DB→app→wire→frontend sweep of int/long/decimal/money after the #2545→#2560→#2575→#2631 money/decimal series (five instances of one root cause — the number wire contract re-decided at every read path per backend). Register: [`numeric-types-audit-2026-08-23.md`](../audits/numeric-types-audit-2026-08-23.md) (F1–F18 + annex; the "what is stable" section is as load-bearing as the findings), with a sibling machine-readable wave plan for fleet execution. **17 missions minted, nothing left unowned**: M-T1.21–M-T1.25 (flutter money end-to-end break ⭐, feliz numerics, the duplicate-`Decimal`-import build break ⭐, money-form seams, money display), M-T2.14 (`columnTypeEqual` blind to precision/scale — #2575's DDL never reaches existing databases ⭐), M-T5.22–M-T5.24 (decimal-arithmetic RS ruling, `long` contract, avg-over-money), M-T6.44–M-T6.48 (leftType-only operand gates ⭐, python decimal coherence, java's missing #2563 twin ⭐, the dotnet hops #2631 didn't reach, numeric ingress 500s), M-T9.36–M-T9.38 (the numeric wire-codec seam + boundary-enumeration gate ⭐, the comparator that can never fail on excess precision, flutter/feliz runtime legs). Wave-1 (9 file-disjoint P1 missions, all broken-codegen or self-contained fixes) is fleet-ready now; embedded design defaults are owner-overridable in draft-PR review.*

*Last refreshed: **2026-08-10** — a full reconciliation pass over the 48 commits since 08-05 (`730741b..48d9257`), all 12 open PRs, and every gap/ratchet register (four parallel code-verified sweeps: merged-commit drift, open-PR coherence + gap harvest, ratchet/waiver inventory, coverage/proposal drift). **Owner directive for this pass: implementation gaps (missing feature per target) and bugs outrank architectural improvements** — §Priority shortlist is rewritten accordingly. Headlines: (1) **the top open item is a live security bug** — a `requires` in a state aggregate's canonical `create`/`destroy` is lowered and then read by NO emitter on any backend, leaving the route open; the whole four-PR cluster around it was mission-less → minted **M-T3.16** with the sequencing and six pending design decisions (#2443 → #2446 → #2487 → new re-port PR; #2450 closes as a vehicle — its "mutation-proven" claim was retracted by its own review). (2) **Duplicate-ID collisions repaired in two track files**: T6 had M-T6.25 ×2 / M-T6.26 ×3 (renumbered; final assignment as merged on main: **M-T6.29** dapper-deny-crash — closed by #2492, **M-T6.30** vanilla-7807-arm, **M-T6.31** absent-read-404), T9 had M-T9.25/M-T9.26 each headed twice (collapsed; M-T9.26 is **unblocked** — #2340 and #2396 both merged). Automation minted as M-T9.32. (3) Status flips: M-T6.26 (`= default`) → `done` (#2440); the vanilla-7807 arm (now M-T6.30's family) closed its RS-9 half via #2472, with #2485 as the booted half (since merged); M-T6.28 → `partial` (root 7807 floor exists; two routers still can't say 409); M-T9.13 → `partial` + re-scoped to the ratcheting `E2E_LESS_CORPUS_FIXTURES` register (8 entries; #2468 drained 3 and found ten bugs); M-T9.17 → `partial` (slice 1 on main); M-T1.3 Phase 1 closed on ALL SIX frontends (#2467 Feliz, #2477 Flutter) with the Chart tail in flight (#2486/#2493); M-T1.11's three "remaining" PRs all merged (new remainder: Flutter sort semantics, grid selection arias → #2497, domain-floor codes); M-T1.12/M-T1.16 `in-flight` → `partial` (no live branches). (4) **Corrections to our own records**: M-T9.25's sweep→rule mapping was wrong in the body (sweep 1 → RS-29, sweep 2 → RS-27-extended; RS-28/RS-30 were never described), M-T9.11's coverage numbers were pre-#2340 (now 36 goldens, 9 waivers — the "2 waivers" claim was stale), M-T5.20 was **silently reverted by #2462 and restored by #2340** (recorded in the mission; the strongest argument yet for M-T9.25 round-2 item 1). (5) The untracked route-builder unification (#2453–#2462, five backends re-rendered from `deriveContextOperations`, 39-tree byte-identical) is recorded as M-T9.26's slice 0. (6) The 08-05 open action is executed: coverage.md's R-rows are minted — **M-T9.28** (repo-wide `.ddd` + clause census; retro §82/§83 re-derived it thrice), **M-T9.29** (driven-primitive census), **M-T9.30** (flake budget), **M-T9.31** (weekly quality delta), **M-T9.32** (dup-claim automation) — plus the M-T9.27 orphan units → **M-T6.32** (stamp codes), **M-T6.33** (misc tails + the waiver-only families), ~~M-T4.11~~ (event-sourcing storage parity — dropped in review: duplicate of **M-T6.34**, which owns it), and **M-T3.15** got its missing track entry. Open-PR attention list rewritten below (of the 08-05 list only #2443 survives).*

*Open-PR attention list (2026-08-10): merge order **#2488 first** among diagnostic-touchers (pure rename of `messages.ts`; #2443/#2446/#2487 all conflict with it), **#2491 before #2486** (both edit `flutter/reads-emit.ts`; 3-file bugfix beats 18-file feature), **#2486 before #2493** (stacked). **#2485 rots fastest** — 48 of 61 files are wire goldens any behavioural PR rebaselines; merge before the next behavioural landing. **#2450: close as a vehicle**, keep its review thread as the design record. **#2492 was a claim-only stub at writing; it since landed the full fix** (merged 2026-08-11 as `9a9263b`, both halves — M-T6.29 `done`). #2489 (test-only) and #2494 (docs-only) are independently mergeable.*

*Corpus audit (**2026-07-30**): a pass over the archived proposals for work the plan never picked up. Findings: (1) **[M-T6.23](T6-backend-parity.md)** minted — `persistence: mikroorm` silently emitted NOTHING for five non-persistence features (query-time projections, timers, realtime SSE, broker channels, the outbox); only one was on record, in a proposal that wasn't in `coverage.md` at all. Honest gates landed; the emitters are the open half. (2) **[M-T5.19](T5-language-core.md)** minted — the test-placement/authoring DSL had shipped THREE phases with no mission tracking it (`coverage.md` said two; Phase 3 is on all five backends). (3) **[M-T9.24](T9-toolchain-health.md)** minted — the 25-bug `fleet-bug-hunt-2026-07-19` register was tracked by nothing; it is part-stale, so re-verify before filing. (Re-verified and then fully drained — all 26 items closed.) (4) `coverage.md`'s "every archived doc is listed" promise had drifted a second time and is now **test-enforced** (`test/system/coverage-guarantee.test.ts`). (5) `read-path-architecture.md`'s "No code yet" header corrected. Prior notes retained below.*

*Last refreshed: **2026-07-28** — a recent-merges reconciliation pass (~50 commits since 2026-07-21), code-verified against `main`. Mission bodies stay owner-maintained PR-by-PR, so the surgical flips were the missions whose **final slice shipped code-only** (no tracker touch): **M-T9.10** `open → done` (Elixir added to the per-feature corpus compile matrix — `test:elixir-corpus` + `corpus-elixir-build.yml`, #2227; silent gap closed), **M-T6.16** `partial → done` (the angular/feliz backend-embed residual it named as "open via #2221" landed), and **M-T7.1** `built → done` (OTel tracing spans on all five backends #2233 — the last piece the 07-21 note flagged as "only OTel spans remain"; per-op counters + collector wiring were already in). Confirmed-current-as-written (no flip needed): **M-T3.2** (authz items 6+7 — `mask unless` read redaction on all five backends + `permission implies` closure), **M-T4.2** (folded + query-time projection on all five, runtime-proven), **M-T6.18** (gap #3 tail — header accurate). CLAUDE.md's corpus list + CI-surface bullet gained the elixir leg. Prior notes retained below. Statuses remain verify-first.*

*Correction + prune (**2026-07-29**): (1) the 07-28 note above briefly listed `write(...)`/`readonly when` write-side field gating as current — it **shipped (#2254) then was reverted the same window** (revert `4d9a7ad`, owner decision: redundant with a row-aware `requires` on the operation). The M-T3.2 mission body in [T3](T3-security-governance.md) is correct ("WON'T DO — reverted"); only read masking (`mask unless`) ships. (2) M-T3.13 (negative-authz runtime gate) is **done** (PR #2259 merged). (3) Resolved a **duplicate-ID collision**: T9's local fuzzing/budget missions had reused `M-T9.12/9.13/9.14`, already owned by the companion [testing-quality-improvement-plan.md](testing-quality-improvement-plan.md) — renumbered them to **M-T9.21/9.22/9.23** (the companion keeps 9.12–9.20). (4) **Pruned all `done` mission bodies** to a one-line header + `Sources:` (the verbose evidence lives in the linked PRs); anchors preserved for cross-referencing live missions. Statuses remain verify-first.*

*Last refreshed: **2026-07-21** — a recent-merges reconciliation pass (~50 commits since 2026-07-14). Two things: (1) **alignment** — [`coverage.md`](coverage.md) had drifted out of its "every archived doc is listed" guarantee; 13 proposals/plans authored after the 2026-07-13 classification (the flutter set, read-path-architecture, test-placement/test-authoring, email-resource-kind, connection-secret-wiring, model-context-pack, loom-verify-tool, unit-tier-domain-test-coverage, playground-flutter-web-preview, nested-state-writes) were never dispositioned — now added, with four mission-less ones flagged. (2) **status review** — mission bodies are largely kept current PR-by-PR by their owners, so the surgical flips were: **M-T7.1** (per-op/domain-fault counters #2150 + collector wiring #2129 landed — only OTel spans remain), **M-T4.2** (query-time projection now emits on the four non-Hono backends, read-path slice 5). Prior 2026-07-14 note retained below. Statuses remain verify-first.*

*Last refreshed: **2026-07-14** — a focused, code-verified status pass (not a full re-classification). Flipped **M-T5.6** (strict decimal/money bounds) `open → done` and reconciled the Priority shortlist annotations against fresh `main` + open PRs (see the dated note under §Priority shortlist). Per-track P1 rows were spot-checked, not exhaustively re-audited — treat every status as verify-first per the rule below.*

## What Loom is building toward

Loom lets you program architecturally correct business apps concisely, with a no-code feel: the `.ddd` model is the single source of truth; UI can be scaffolded from the domain and customized through escape hatches (`extern`, unfold); the model is editable as text, visually (builder), or by an AI agent through the compiler's tool surface; and backend/frontend targets are a config choice. The plan below is organized so that every open thread in the old corpus lands in exactly one **mission** — a self-contained, agent-pickable unit of work.

## How to use this plan (for agents)

**Starting a mission?** The full execution protocol lives in [`RUNBOOK.md`](RUNBOOK.md) — the kickoff prompt for any mission is two lines (mission ID + "follow the runbook"). The rules below are the summary; the runbook is the contract.

1. **Pick a mission** from a track file (`T1`–`T10`). Missions are sized S (≤1 PR, hours), M (1–3 PRs), L (a PR stack / multi-session), XL (an epic with its own sub-plan).
2. **Re-verify before building.** Statuses here were classified 2026-07-13, and `main` moves fast. A mission's first step is always: check fresh `main` (and open PRs) that the gap still exists. Missions carrying a ⚠ *verify-first* flag have known doubt.
3. **Claim with a draft PR** naming the mission ID (e.g. `M-T1.1`) before implementing — see CLAUDE.md's claiming protocol.
4. **Honor the D-tags.** Pinned decisions in [`../decisions.md`](../decisions.md) constrain design; a mission that contradicts one needs the decision re-opened first, not silently ignored.
5. **When a mission completes**, update its status line here (and the coverage row if the source doc is now fully drained). This plan is the only status table now — don't resurrect the old ones.

## Status legend

`open` (no code yet) · `in-flight` (claimed/branch exists) · `partial` (some slices landed; mission covers the remainder) · `blocked(X)` (waiting on mission/decision X) · `done` (kept briefly for context, then delete the entry).

## The tracks

| Track | Theme | Weight |
|---|---|---|
| [T1 — UI & frontend ceiling](T1-ui-frontend.md) | Data-heavy tables, upload, forms tail, state/async, i18n, a11y, extern parity, navigation | **P1 — highest product ROI** |
| [T2 — Data & schema evolution](T2-data-evolution.md) | Rename intent, data migrations, baseline safety, seeding/uniqueness tails, storage config tail | **P1 — the "silent data loss" class** |
| [T3 — Security, tenancy & governance](T3-security-governance.md) | Deny-by-default, find gating, authorization items 3–7, org-context, OIDC depth, sensitivity, versioned-on | **P1 — secure-by-default** |
| [T4 — Eventing, workflow & temporal](T4-eventing-temporal.md) | Timers/jobs ⭐, projections, channels/brokers, outbox completion, saga hardening, email/storage batteries | P2 (temporal hole is the biggest single gap) |
| [T5 — Language core & type system](T5-language-core.md) | Exception-less A4–A6, criterion/retrieval tails, payload P3/P5, stdlib tail, inheritance I4, lifecycle 3–5, surface hygiene | P2 |
| [T6 — Backend parity & generated-code quality](T6-backend-parity.md) | Phoenix gaps register, Java crash gates, SPA-embed under Phoenix, adapter subsets, live review-remediation slices, SYS-1 | P1/P2 (small missions, wrong failure modes today) |
| [T7 — Deployment & operations](T7-deployment-ops.md) | Metrics/OTel, k8s hardening, proxy/networking, terraform, PaaS deploy | P2 |
| [T8 — DX, tooling & the AI platform](T8-dx-tooling-ai.md) | Debugger frontier, sourcemaps, LSP tail, playground chat/agent loop, builder, packaging split, mutation testing | P2/P3 |
| [T9 — Toolchain & process health](T9-toolchain-health.md) | Langium 4 ⭐, persistence-emit seam ⭐, per-PR boot gates, test-coverage phases, doc hygiene | **P1 — prerequisite to growing the matrix** |
| [T10 — New targets](T10-new-targets.md) | Feliz completion only; Go/PHP/NestJS/Blazor/HTMX/Next.js studies **retired to design-record**; **matrix frozen — decided 2026-07-17, no more targets** | — (closed) |

## Sequencing — the load-bearing dependencies

- **The governance spine is mostly built.** execution-context backbone, multi-tenancy Phases 1–2, authorization read/write ladders + named policy fns all ship — and (updated 2026-08-05) item 3 (operation/view/workflow/find gates) and item 6's read half (`mask unless`, all five backends) are **done**, with item 6's write side **WON'T DO** (reverted #2254/#2257). What remains, in order: P4 `deny` (M-T3.3) → `organizationContext` (M-T3.6 — its hard prereq, the item-3 gate machinery, is now met).
- **Coordinated single-PR moments** (one PR + fixture re-baseline, don't slice): **A4** `Repo.getById` re-shape to `T or NotFound` (M-T5.1); **versioned default-on** (M-T3.4, breaking wire change); **paged-by-default implicit findAll** (M-T2.6, breaking change to every list endpoint).
- **Target freeze — decided (2026-07-17):** the matrix is permanently closed; **there will be no more backends or frontends** (owner decision, see [direction-review-2026-07](../audits/direction-review-2026-07.md)). This supersedes the earlier "don't start before M-T9.2" gating — M-T9.2 concluded the persistence surface can't be abstracted, so a growing matrix would have re-landed it by hand forever; frozen, that cost is bounded ×5 and amortizes. The in-flight Feliz frontend completes as a committed target; the T10 studies are design-record, not backlog. **The breadth budget redirects to depth (T4 temporal, T2 data-evolution) — each capability now lands ×5 once against a closed set, then is done.**
- **Ordering within a track is top-to-bottom** unless a mission states a dependency.

## Priority shortlist (if you only take five things)

**Architecture-risk work outranks feature breadth.** The [weak-spot review](../audits/architecture-weak-spots-2026-07.md) is the ranking authority: anything it names as a structural risk (silent data loss, silent/crash parity failure modes, security defaults, the un-abstracted persistence axis, the nightly-only feedback loop) is P1 by default, ahead of new surface area. We strive for excellence — a smaller platform whose claims all hold beats a wider one with hollow cells.

> **[2026-08-10 rewrite]** Owner directive: **gaps in implementations (missing feature per target) and bugs are highest priority now, then architectural improvements** — while keeping the rest tracked. The 08-05 shortlist is superseded (its row 1 M-T3.6/M-T3.3 governance work stays open but no longer outranks live defects; its row 5 tracker-hygiene items are done — #2340 landed, M-T9.26 unblocked, the R-row missions are minted). Same rule as ever: verify against fresh `main` + open PRs before claiming.

1. **M-T3.16 — the lifecycle `create`/`destroy` guard is silently non-enforcing (all five backends, route left OPEN).** The one P0. Sequence: #2443 (hoist the operation gate; lands first, also creates `op-gates.ts`) → #2446 (the honest error) → #2487 (the readable-surface contract) → a NEW re-port PR for the emission (close #2450; six design decisions listed in the mission are pending, the Elixir context-vs-controller one first).
2. **The persistence-adapter gap axis — ~~dapper `policy { deny }`~~ (`done`, #2492 → M-T6.29: both arms emitted, ratchet back at 2), M-T6.25 (dapper query-time projections: 2 shapes ratcheted, 2 with no fixture at all — write the witnesses first), then M-T6.23's five mikroorm emitters (outbox merged #2516; broker/timers/projection/realtime in flight #2524/#2525/#2533/#2534).** The compile matrix is clean on the five backends; the remaining compile-tier debt lives on this axis.
3. **The intra-backend split family — ~~M-T6.27~~ (`done`, #2505 — elixir's named-op path now CAS-guards and 409s), M-T6.30 (absent-read 404 is three envelopes: dotnet factory-shape, java empty-body), M-T6.28 residue (two node routers whose typed ladder cannot express 409).** Each needs its two-writer / missing-id golden — the coverage gap is why they shipped.
4. **The frontend feature×target tail:** flutter's 4 pinned form-field shapes; `money.min/max/round` unusable in page bodies (#2489's harvest, no gate yet); the walker's remaining multi-child single-slot reads (`Tab`/`KeyValueRow`/`Stat`/`Modal` — the class #2567 fixed for `Card`). *(Two rows removed 2026-08-17, both shipped: `CHART_FRAMEWORKS` now lists **all seven** frontends — `system-checks.ts:416-424` — so the `Chart` tail is closed and the scaffolded dashboard tile is unblocked, which is the M-T1.3 remainder; and `KNOWN_DEGRADATIONS` (`test/generator/_walker/render-degradation.test.ts:92`) is **EMPTY** — the angular/feliz user-component drop was fixed by `angular/components-emit.ts` + `feliz/component-emit.ts`.)*
5. **Coverage that HIDES gaps (the enabling layer):** M-T9.13's drain of `E2E_LESS_CORPUS_FIXTURES` — the register holds **9** entries as of 2026-08-17 (`test/ir/api-caller-census-pins.ts:469-531`), not the 7 this row used to claim; **two of the nine are now drainable**, because the blocker their own entry text names has shipped: `policy-document` cites the multi-principal harness (#2515, landed) and `lifecycle-guard` cites both that harness and "no negative-status assertion form", which the `AUTHZ_LADDERS` three-rung walk supplies on all five legs post-#2570. The remaining seven are sidecar/two-deployable/fixture-change shapes with checked reasons. Then: M-T9.28's repo-wide `.ddd`/clause census (three independent recurrences in six days); register the unwatched ratchets in `allowlist-ratchet` (M-T9.8).

**Architectural improvements queue behind these** (explicitly still tracked, in order): M-T9.26 `RouteTarget` (now unblocked; re-measure post-#2462 first), M-T5.21 callable unification (design signed off in #2444), M-T3.6 `organizationContext` + M-T5.1 A4 re-shape (the two remaining coordinated moments), M-T9.25 round 2 (the under-an-override census — item 1 is now evidence-backed by the #2462 silent revert; **item 2, the 401/403 arms, is no longer blocked** — the harness landed in #2515 and the ladder runs on all five behavioural legs).

## Statuses rot — verify, then verify the verifier

Two standing rules beyond the per-mission verify-first step:

- **No status flip without code evidence.** Marking a mission `done` requires the PR link *and* the gate/emitter/test evidence line — the same standard the old corpus failed to keep (its three status tables drifted apart within weeks).
- **Audit for pretended work.** In a repo where parallel agents land PRs continuously, "merged" is not "real": gates get softened, dead code gets left unwired, TODOs get emitted into output. **M-T9.8** is the recurring adversarial sweep for this class; run it after any large multi-agent push.

## Provenance & coverage

Every archived proposal/plan is dispositioned in [`coverage.md`](coverage.md): either *shipped/superseded/historical* (no open work) or mapped to the mission(s) that carry its remaining items. If you find an open thread in an old doc that no mission covers, that's a bug in this plan — add a mission, don't fork a new tracker doc.

Audit findings feed the same way: the open items from `completeness-audit-2026-07`, `architecture-weak-spots-2026-07`, `full-code-review-2026-07` (#6, #22, C-mediums), `generated-code-ddd-review-2026-07` (S5d, S10 tail), `generated-code-review-2026-06-30` (SYS-1), and `showcase-coverage-bugs` (BUG-003/004) are all mission-mapped; see coverage.md §Audits.
