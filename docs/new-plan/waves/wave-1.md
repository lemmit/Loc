# Wave 1 — close the P0/P1 silent residue (coordinator log)

*Plan: [`../improvement-waves-2026-09.md`](../improvement-waves-2026-09.md) §Wave 1 and §3a. Base: `main` @ `b826f87` (#2713 merged). One PR for the wave; packets work on `claude/wave-1-<packet>` sub-branches (git refuses `claude/wave-1/<packet>` while `claude/wave-1` exists) and are folded here by the coordinator. This file is the claim: any agent reading the PR list sees every packet, row and tree fence below. Hand-off notes are appended verbatim under §Hand-offs as packets land.*

## Packets

| packet | model | tree fence | rows (in fold order) | state |
|---|---|---|---|---|
| 1a validator + cli | Opus | `src/cli/**`, `src/ir/**`, `src/diagnostics/**`, `src/language/validators/**` | `ir-warnings-invisible-in-cli` → `F2-VAL-1` → `F2-ADP-3` gate → `timer-tz-overlap-inert` gate → `eventlog-shape-silently-ignored` → `M-T5.9-reserved-not-emitted` → `M-T6.18-gap3-criterion-arg-types` → `M-T5.25` → `F2-CB-C9` / `F2-CB-C10` / `F2-EXPR-5` messages → `M-T3.8` diagnostic slice | **folded** — `F2-EXPR-5` fixed; `M-T3.8` diagnostic slice landed (`loom.sensitive-wire-unsupported`); `ir-warnings` residue fixed in `ddd snapshot`/`verify` (one `printLoomWarnings` for four commands); `scaffold-filter-param-unsupported` wording fixed; 9 rows already-done-verified by RUNNING the repros (incl. `F2-ADP-3` — W1b landed the gate, so the ledger now has **zero open P0s**); `F2-CB-C9` handed off (one arm in `type-system.ts`) |
| 1b dotnet-adapters | Opus | `src/generator/dotnet/**` | `F2-ADP-1` (P0) → `F2-ADP-4` → `F2-ADP-2` → `G2667-D3` → `M-T3.9-dotnet-audit-masked-snapshot` (VF) → `G2667-D4` (VF) → `dapper-no-schema-evolution` (decision: honest gate this wave) | **folded** `d449b7e` — 1 fixed (D3 dotnet arm), 5 already-done-verified (#2668/#2708), 2 handed off |
| 1c node-ts | Opus | `src/platform/hono/**`, `src/generator/typescript/**` | `F2-ADP-5` (P0) → `F2-ADP-6` → `F2-CB-C8` → `F2-EXPR-4` → `M-T6.51` → `F2-W-05` (VF) → `F2-CB-C1-paged-nonrelational` (VF) → `G2667-C2-money-array` (VF) → `M-T5.14` → `static-subpath-405-node-only` | **folded** — `G2667-D3` node arm fixed (LEFT-JOIN, both adapters); two residues fixed (audit trail `at` still `.000Z`; mikro durable outbox row committed outside the save tx); `F2-ADP-5` was fixed but unpinned on node — 16 write-scope cases added; 8 rows already-done-verified; `M-T5.14`/`static-subpath-405` handed off with per-backend shapes |
| 1d elixir | Opus | `src/generator/elixir/**` | `F2-ELX-ESCAPE-FUNNEL` (P0) → `F2-W-01` → `F2-FFE-6` → `M-T6.26-doc-put-presence` → `elixir-grapheme-vs-codepoint-length` → `F2-MT640-SORT-DEAD` → `M-T6.2-s14-audit-wiresnapshot` → `G2667-C7` (decision) | **folded** — the four audited `#{` sites were fixed, but **five more live splices** of authored `.ddd` strings into Elixir source were found and closed (VO invariants on both Ecto carriers, page `state` initialisers, handler guard flash, OIDC `issuer:`/`clientId:`, claim path/scopes, storage config) — compile-verified under `--warnings-as-errors`; `G2667-C7` rendered; `G2667-D6` fixed (re-verified open; #2719 had not closed it); `G2667-D3` elixir arm LEFT-JOINed; 7 rows already-done-verified; `F2-W-08`/`W-09` handed off to wire-openapi |
| 1e python + macros | Sonnet | `src/generator/python/**`, `src/macros/**`, `src/generator/_walker/primitives/text.ts` | `M-T6.50` (migrate onto `walk.ts`) → `M-T1.15-nonstring-filter-finds-dropped` → `provenanced-bare-read-in-page-body` → `M-T1.26` | **folded** — M-T6.50 fixed (three collector gaps; collector on `walkStmtExprsDeep`), `G2667-D3` python arm fixed (LEFT-JOIN, matches 1b), M-T1.26 fixed on react/svelte/feliz/flutter (vue/angular need a `designs/**` pack change — handed off), M-T1.15 already-done-verified (#2698), `provenanced-bare-read-in-page-body` handed off with the patch shape (walker-core, out of fence) |
| 1f frontend-js | Sonnet | react / vue / svelte / angular generators, `designs/**` | `F2-CFE-2` → `F2-CFE-8` → `M-T1.8-error-boundary-five-targets` → `M-T1.12-raw-field-aria` → `G2667-D8` | **moved to Wave 2** — #2720 is still open (`mergeable_state: dirty` against `main`) and already covers `G2667-D8`, `M-T1.12`, the svelte/angular halves of `M-T1.8` and gates `F2-CFE-8`; the residue (`F2-CFE-2`, `M-T1.8` on feliz/flutter/HEEx, `M-T3.15-C3`) is re-scoped after #2720 lands. **Merge-order note:** #2720 and this wave both bump `MAX_OPEN_GAPS` in `unsupported-register.ts` (+2 and +1) and both add firing-census fixtures — whichever merges second re-bases that pin. |
| 1g SSE auth | Opus | `src/generator/_frontend/realtime.ts`, `src/ir/util/realtime-rooms.ts`, per-backend SSE plugs | `M-T4.12` | **folded** — contract stated in `realtime-rooms.ts` (stream authenticates like an ordinary API call: session cookie + `withCredentials`); 4 SPAs + feliz + node verifier fixed; flutter handed off (no credential path at all) |
| 0.2 ledger reconciliation | Sonnet | `docs/audits/targets-completeness-2026-08-30.*`, `docs/new-plan/T9-toolchain-health.md` | move W1/W1b/W2/#2719/#2726 rows to `done`; recount the `.md` header from the JSON; `M-T9.36 → open (unblocked)`; record the `F2-ADP-3` handoff | **folded** — 63 rows → done, 15 → claimed; `scripts/ledger-counts.mjs --check` + `test/system/ledger-counts.test.ts` make the counts code. **Correction to the plan's §0:** #2668 (the audit PR) drained `F2-ADP-1`, `F2-ADP-5`, `F2-ELX-ESCAPE-FUNNEL`, `ir-warnings-invisible-in-cli`, `F2-VAL-1` and more in the same PR that cut the ledger, without moving them out of `open`. The only open P0 is the `F2-ADP-3` gate. Ledger now: open 169 (P0 1 / P1 11 / P2 20). |

VF = verify-first: a merged PR body mentions the id; re-derive on this base before building.

## Fold protocol (coordinator)

1. Packet hands over `claude/wave-1-<packet>` + a hand-off note (row table: fixed / gated / handed-off / already-done-verified; the mutation-proof assertion per fix; local gate results).
2. Coordinator merges the sub-branch into `claude/wave-1`, runs `npx tsc -b`, `npm test` and the packet's compile leg on the folded tree, appends the note under §Hand-offs, and pushes — at most once a day.
3. Rebase onto `main` only on a real conflict. Flip to ready once, when the whole wave is green locally.

## Hand-offs

*(appended as packets land)*

- **1b dotnet-adapters** — [`handoffs/wave-1-dotnet-adapters.md`](handoffs/wave-1-dotnet-adapters.md). Hand-offs raised: the `G2667-D3` LEFT-JOIN ruling + the four other backend arms; `dapper-no-schema-evolution` needs a phase-⑨ owner (`src/system/migrations-builder.ts`, plus the misdirected M-T2.2 baseline error); `F2-EXPR-7` ruling proposed (`first: T` ⇒ throws on empty; node/elixir are the degraders); `F2-W-14` confirmed, not landed (runtime-only proof).
- **1g SSE auth (M-T4.12)** — [`handoffs/wave-1-sse-auth.md`](handoffs/wave-1-sse-auth.md). Touched two files in other fences (`hono/v4/auth-emit.ts` — node read only the `Authorization` header, so a cross-origin `auth: ui` SPA on node could authenticate nothing; and the four SPA `index.ts` call sites). Hand-offs: flutter needs a bearer rule (an HttpOnly cookie cannot exist on mobile — a plan amendment); a cross-backend emitter test that every SSE route sits behind the auth plug; no browser-level leg drives a real `EventSource`. Both OIDC runtime legs extended; Keycloak cannot boot in the sandbox, so the assertions were proven against a live generated node backend with the mock issuer.
- **0.2 ledger reconciliation** — rows already fixed that packets were told to skip: 1a `ir-warnings-invisible-in-cli`, `F2-VAL-1`, `M-T5.9`, `M-T6.18-gap3`; 1c `F2-ADP-5`, `F2-ADP-6`, `F2-CB-C8`, `F2-EXPR-4`, `F2-W-05`, `G2667-C2`; 1d `F2-ELX-ESCAPE-FUNNEL`, `F2-W-01`, `F2-FFE-6`, `M-T6.26`, `elixir-grapheme-vs-codepoint-length`; 1e `M-T1.15`. Lesson for the next wave plan: a ledger's `open` bucket is a claim, not a fact — the PR that cuts a ledger can drain rows in the same diff; re-derive against tests before scheduling.
- **1e python + macros** — [`handoffs/wave-1-python-macros.md`](handoffs/wave-1-python-macros.md). Hand-offs: `provenanced-bare-read-in-page-body` → walker-core (`heex-walker-core.ts`'s `provenancedFieldNames(ctx)` already solves the mirror lookup; patch shape in the note); M-T1.26 vue/angular arms → `designs/**` pack templates; a stale message text for `loom.scaffold-filter-param-unsupported` (still claims bool/datetime/guid unsupported) → packet 1a.
- **1c node-ts** — [`handoffs/wave-1-node-ts.md`](handoffs/wave-1-node-ts.md). Open questions raised: B20 (a channel-less `projection … on(Event)` now folds on NO backend — implicit in-process subscription or a refusal; `enrichments.ts:1303`); the RS rule for `G2667-D3`'s absent value (node `null` vs .NET `default!`); outbox capture is relational-only on both node adapters (a durable channel on a non-relational aggregate is at-most-once, silently — wants a ledger row); sub-second datetime wire form diverges across backends (`.12Z` / `.120Z` / `.120000Z`) and `response-diff.ts` normalizes it away.
- **1a validator + cli** — [`handoffs/wave-1-validator-cli.md`](handoffs/wave-1-validator-cli.md). Method note worth keeping: 1a closed three rows the reconciliation had left open (`F2-ADP-3`, `timer-tz-overlap-inert`, `eventlog-shape-silently-ignored`) by RUNNING each repro — a reconciliation that reads PR bodies finds what a PR claimed; running the repro finds what it did. Also: a ledger row's `fileTrees` is a sample, not a census (`ir-warnings` named two of four defective commands).
- **1d elixir** — [`handoffs/wave-1-elixir.md`](handoffs/wave-1-elixir.md). The Wave 2.2 input: every remaining bare `JSON.stringify` in the elixir emitters is classified by what reaches it (ID-terminal names, compiler-built strings, NUMBER text, rendered SQL, one internal string); `seed-emit.ts` `exStr` duplicates `elixirString` (delete); and `elixirString` has no enforcement — nine live injection sites were found by reading call sites twice, which a lint rule / `test/system` scan should do. Hand-offs: the LEFT-JOIN RS rule; `T6-backend-parity.md` `F2-MT640` row reads `open` but no longer reproduces; a hostile-`#{` fixture for `test/e2e/fixtures/elixir-vanilla-build/`; `F2-W-09` wants one owner across backends.
- **1h sweeper** — [`handoffs/wave-1-sweeper.md`](handoffs/wave-1-sweeper.md). `F2-CB-C9` fixed (the `<DomainService>.<op>(...)` arm in `typeOfPostfixChain`); `M-T1.26` now fixed on all six JS-family targets (vue/angular through the pack templates and two pure helpers in `_packs/loader.ts`, not `_walker/**`).
