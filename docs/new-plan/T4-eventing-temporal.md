# T4 — Eventing, workflow & temporal

*Weak-spot #4: scheduling was the language's temporal hole — no timers, no jobs, no saga deadlines. `timerSource` Phases 1 + 2 now close the timer half on all five backends (in-process scheduler + advisory-lock single-fire, plus durable Postgres-backed `cron:` drivers with store-coordinated single-fire, retry, and coalesce-once missed-run catch-up; only timezone/overlap sugar + saga deadlines still pending). Deployables are no longer pure runtime islands: external-broker transport (M-T4.4) now provisions cross-service eventing — Redis, RabbitMQ (`queue`/`work`), and Kafka (`broadcast`/`log`) legs have landed across the backends (Kafka fan-out is complete except the Elixir leg; channel auth + k8s remain, so M-T4.4 stays in-flight). The saga/workflow core is otherwise solid and at full 5-backend parity.*

## M-T4.1 — `timerSource` (scheduling) — `partial` · **XL** · P1 (design-first)
The temporal hole. Time as an event source: a system-scope `timerSource` binding emitting tick events; workflows react via existing `on(e)`/`create(e) by` triggers — zero new workflow grammar (the `schedule every 5m {}` shape was explicitly rejected). Phase 1 (in-process scheduler + advisory-lock single-fire) AND Phase 2 (durable per-backend `cron:` drivers) are both landed on **all five backends**; only timezone/overlap sugar + saga deadlines remain.
**Phase 1 landed — all five backends:** grammar (`timerSource` SystemMember + `DURATION` terminal + system-wide `for:` scope), `TimerSourceIR`, cadence validators (`loom.timer-cadence` AST-level; `loom.timer-event-shape`/`-needs-state`/`-source-unbound` IR-level), print arm, obs catalog (`timer_fired`/`timer_skipped_overlap`/`timer_lock_contended`/`timer_emit_failed`). Owner derived from `migrationsOwner` (no stamp); byte-identical where unused. Per-backend driver, all sharing the tx-scoped `pg_try_advisory_xact_lock` single-fire (same FNV-1a→int32 key, so cross-backend replicas contend on one key) and dispatch through the existing in-process saga dispatcher:
- **Hono/node** (#1963) — `scheduler.ts`, node-cron (`cron:`) / setInterval (`every:`). `examples/timers.ddd`.
- **.NET** (#1986) — `TimerService : BackgroundService`, Cronos / `PeriodicTimer`, EF Core `SqlQuery<bool>`.
- **Java/Spring** (#1987) — `TimerScheduler @Component`, `@Scheduled(cron)` / `@Scheduled(fixedRate)`, `TransactionTemplate`. No new Gradle dep.
- **Python/FastAPI** (#1988) — `app/scheduling.py`, APScheduler `CronTrigger` / `IntervalTrigger`, `apscheduler>=3.10,<4`.
- **Elixir/Phoenix** (#1989) — one GenServer per timer, `Process.send_after` (`every:`) / `Crontab.Scheduler` (`cron:`, conditional `{:crontab}`); serialized GenServer loop makes overlap structurally impossible (no `timer_skipped_overlap`).
**Phase 2 landed — durable `cron:` drivers, all five backends.** A `cron:` timer becomes a durable, Postgres-backed job (store-coordinated single-fire across replicas + retry + coalesce-once missed-run catch-up); `every:` (sub-minute) timers stay the in-process advisory-lock path. Each backend uses its ecosystem's Postgres-native store; every driver was prototyped against Postgres, then runtime-verified on the *generated* stack (boot → cron fires → tick dispatches → saga persists; first-boot baseline + pushed-back-watermark catch-up asserted). Adds the `timer_catchup` obs event.
- **node/Hono** (#2009) — pg-boss (`boss.schedule`/`boss.work`, retryLimit 3) + a self-owned `loom_timer_runs` watermark for coalesce-once catch-up (pg-boss cron has no native backfill).
- **.NET** (#2101) — Hangfire + Hangfire.PostgreSql; standard cron, auto-schema, native overdue-on-start missed-run; `IRecurringJobManager`.
- **Java/Spring** (#2112) — JobRunr core (no SB4 starter → manual `JobRunrConfig`: SQL `StorageProvider` + background server + `scheduleRecurrently`).
- **Python/FastAPI** (#2132) — procrastinate `@app.periodic` tasks; the `periodic_defers` table is the store-coordinated single-fire + native coalesce-once catch-up ledger; worker runs in-process in the lifespan. Deps swap apscheduler → `procrastinate` + `psycopg[binary]`.
- **Elixir/Phoenix** (#2135) — Oban; a scheduler GenServer enqueues unique (per-boundary) Oban jobs (`unique` = the single-fire ledger, never re-runs a boundary) + a `loom_timer_runs` watermark for coalesce-once catch-up.
**Remaining:** timezone `in:` + `overlap: allow` sugar; Phase 3 (`missed: run`/`retry:` surface syntax, per-env cadence, saga-deadline sugar, `ddd verify` join).
**Runtime-verified (Hono, docker postgres):** the timer fires on cadence → tx-scoped `pg_try_advisory_xact_lock` (single-fire; no lock leak under concurrent load — the fix over a plain session lock, which leaks the unlock across pooled connections) → tick dispatched through the in-process channel dispatcher → `create(t) by t.<id>` reactor creates one saga instance per tick → persisted + readable via `/api/workflows/.../instances`.
**Note — reactor correlation:** a tick reaches a workflow only through a `channel` carrying it, and workflow event-consumers require an id-shaped correlation field; the scheduler mints tick fields (`newXId()` / `new Date()`), so `create(t) by t.<id>` spawns one instance per tick. Relaxing correlation for stateless recurring `on(t)` is a Phase-2/3 consideration.
**Note — delivery is at-least-once:** the advisory lock is a mutex around the body (no concurrent double-fire), but clock-skewed peers can fire sequentially, so reactors must be idempotent. Exactly-once-per-instant (durable claim / competing-consumer) is deferred to Phase 3 — not needed for the at-least-once contract.
Sources: [scheduling.md](../old/proposals/scheduling.md), weak-spots §4, completeness-audit Tier 1.

## M-T4.2 — `projection` read models — `partial` · **L** · P2
`projection <Name> keyed by <field>` folded from foreign events. Verified 2026-07-13: the grammar + the **Hono runtime** landed (v1 slice 2, #1732); the other four backends didn't consume the kinds yet — papered over by the showcase allowlist rather than gated (see M-T6.16). **Cross-backend emit progressing:** the **query-time projection** path (the read-path-architecture generalization of `projection`) now emits on the four non-Hono backends — read-path slice 5 PR-D Python (#2117), PR-E Elixir (#2171), PR-F Java (#2190), PR-G .NET (#2195). Remaining: folded-mode runtime parity across the four backends, `GET /projections/<name>` parity, the 8 validators. Deferred v1.1: projection-as-view-source, replay/rebuild (needs durable log), snapshots.
Sources: [projection.md](../old/proposals/projection.md) (draft 2026-07-05), [read-path-architecture](../old/proposals/read-path-architecture.md) (query-time projection generalization), [workflow-and-applier](../old/proposals/workflow-and-applier.md) §projections, production-readiness §3.5.

## M-T4.3 — Outbox & delivery completion — `partial` · **M** · P2
Outbox + idempotent-consumer markers ship on Hono/.NET/Python (+Java ⚠ verify). Remaining: Phoenix/Oban relay, S5(d) close-out from the DDD review, LISTEN/NOTIFY upgrade over polling, dead-letter surface + observability event, the opt-in knob decision (reuse `retention:`).
Sources: [dispatch-delivery-semantics](../old/proposals/dispatch-delivery-semantics.md), ddd-review S5(d).

## M-T4.4 — Cross-deployable eventing (external brokers) — `done` · **XL** · P2 (slice plan complete 2026-07-21; residuals: java/elixir saga `last_event_id` dedup — broker-ack stance recorded in 7c/7d; M-T4.2 replay-cursor hook stays future)
`channelSource` parses but is "declared, not provisioned" — a microservice-shaped system is decorative. Provision brokers from `channelSource`, emit producer/consumer clients, `delivery: queue` competing-consumer semantics, service-to-service auth. Queue-semantics implementation sequences after M-T4.3 (producer rides the outbox relay). **Broker rollout pinned (2026-07-18): redis → rabbitmq → kafka, one slice each; NATS dropped** (stays a parseable storage type — `loom.channelsource-unsupported-transport` rejects it; ancestor-room routing rides Redis patterns / RabbitMQ topic wildcards, never Kafka). Licensing pinned free/OSS-only (design §6a: Valkey image, `apache/kafka`, no MassTransit). Excludes browser delivery (M-T1.10) but owns the `ChannelTransport` publish/subscribe seam both missions consume. Kickoff: [missions/M-T4.4-broker-eventing-brief.md](missions/M-T4.4-broker-eventing-brief.md); design (signed off): [missions/M-T4.4-broker-eventing-design.md](missions/M-T4.4-broker-eventing-design.md).
**Slice 1 landed:** deployable `channels:` wiring clause (grammar + `DeployableIR.channelSourceNames` + print arm + `channels` soft keyword), post-NATS compat matrix + `loom.channelsource-unsupported-transport` (AST), wiring validators `loom.channelsource-unbound`/`loom.deployable-channel-unrelated`/`loom.channel-consumer-unwired` (IR), shared vocabulary `src/util/channels.ts` (matrix + `loom.<ctx>.<chan>` addresses + consumer groups + CloudEvents envelope field pin), asyncapi `wiredBy:`. **Also unlocked cross-context reactors:** `on(e: X)`/projection-fold event refs now resolve system-wide (targeted scope arm mirroring `timerSource for:`) — previously context-local, which made the proposal's cross-deployable choreography inexpressible. Merged: #2035.
**Slice 2 built (PR #2050):** Redis `broadcast`/`ephemeral` transport — `valkey/valkey:8-alpine` compose sidecar (§6a licensing) + `LOOM_CHANNEL_*_URL` env injection, generated `http/channels.ts` on Hono (reference driver): `ChannelTransport` seam, CloudEvents envelope, ioredis pub/sub, producer tee enforcing the §4 delivery-uniformity rule (broker-bound events publish; co-located consumers receive via subscription), consumer loop into the in-process dispatcher, obs `channel_*` events. Cross-deployable vocabulary: a wired-but-foreign channel's events join the consumer's `domain/events.ts` (+ id brands); `create(e: X)` starters resolve foreign events (scope widening + system-wide member typing, the create-position twin of slice 1's `on(e: X)` arm); cross-context `X id` refs drop their FK in migrations (id-only linkage — the target lives in another schema/database). Landed as #2050 (e139804). Runtime-proven by the opt-in `channels-e2e` gate (`npm run test:channels`): two generated deployables, separate databases, docker valkey — an `Order.place()` emit on the producer spawns the correlated Fulfil instance + Shipment on the consumer.
**Slice 2b built (PR #2078):** the Python (FastAPI) leg of the same transport — generated `app/channels.py` (CloudEvents envelope codec over the DSL field names, redis.asyncio pub/sub, `publish_event`, consumer loop feeding `InProcessDispatcher`), `ChannelTeeDispatcher` inside `make_dispatcher` (a subscription-less pure producer gets a minimal `dispatch.py` so drained events still reach the tee), FastAPI lifespan boot/close wiring, wiring-gated `redis` dep, foreign event dataclasses + id brands + cross-context id imports in routes. Runtime-proven CROSS-BACKEND by the opt-in `channels-e2e-python` gate (`npm run test:channels-python`): the generated Hono producer publishes, the generated FastAPI consumer (own database) receives the same envelope id and spawns the correlated Fulfil instance + Shipment — two backends, one wire contract.
**Slice 3 built (PR #2082):** RabbitMQ `queue` transport (Hono reference driver) — `rabbitmq:4-management-alpine` compose sidecar (MPL 2.0, §6a) + amqplib (MIT), §4 topology (per-address exchange, one durable queue per consuming deployable = `<address>.<deployable>`, replicas compete), manual ack → bounded retry → DLX `loom.dlx` → `loom.dlq.<channel>` parking + dead-letter obs event (co-designed with the M-T4.3 dead-letter item), envelope-`id` idempotency on consume; producer path split per design §5 — `queue`/`work` events ride the outbox relay (publish on drain), `queue`/`ephemeral` keeps the inline tee. Runtime-proven by the opt-in `channels-e2e-rabbit` gate (`npm run test:channels-rabbit`): 6 events published via the outbox relay are consumed exactly once across 3 competing replicas (observed 2/1/3 split), and a poisoned message parks in `loom.dlq.<address>` with the `channel_dead_lettered` obs event. A workflow-less durable-broker producer now emits the outbox machinery + relay (new pure-producer shape). Known follow-up: consumer-side envelope-id dedup markers only engage where the consumer HOSTS the durable channel's context (the foreign-channel consumer relies on rabbit ack semantics + idempotent reactors for now).
**Slice 6a built (PR #2087):** backend fan-out — the .NET (ASP.NET Core) redis `broadcast`/`ephemeral` consumer/producer leg over the shared `ChannelTransport` seam: generated `Channels.cs` (StackExchange.Redis, MIT — §6a; envelope codec over DSL field names), publish tee in the dispatcher chain, consumer `BackgroundService` feeding the existing in-process dispatch, foreign event vocabulary + id brands, wiring-gated dep, compose already platform-agnostic. Runtime-proven cross-backend in the sandbox (SDK container): the Hono producer published envelope → Valkey → the .NET consumer received the SAME envelope id, spawned the correlated Fulfil instance and persisted the Shipment in its own database; `dotnet build /warnaserror` clean. Gated by generator pins + the opt-in `channels-e2e-dotnet` suite (`npm run test:channels-dotnet`, needs the .NET SDK on PATH — the tenancy-isolation-dotnet pattern). Remaining fan-out: Java, Phoenix (redis broadcast) + Python/.NET/Java/Phoenix rabbit legs.
**Slice 6b built:** backend fan-out — the Java (Spring Boot) redis `broadcast`/`ephemeral` leg over the shared binding resolution: generated channel classes under `config` (CloudEvents envelope + per-event codec over the DSL field names, Lettuce pub/sub driver — Apache 2.0, §6a), a `DomainEvent`-typed publish-tee `@EventListener` (§4 delivery uniformity: dispatcher handlers for broker-routed events DROP their local `@EventListener` — the `ChannelConsumerService` invokes those same methods on delivery, off the driver's event loop through a single-thread executor), wired-but-foreign channels widening the per-context dispatcher derivation, foreign event records + id brands, wiring-gated `lettuce-core` dep (channel-less projects byte-identical). Runtime-proven CROSS-BACKEND by the opt-in `channels-e2e-java` gate (`npm run test:channels-java`): the generated Hono producer publishes to valkey and the generated Spring Boot consumer (own database) receives the same envelope id, spawns the correlated Fulfil instance + Pending Shipment; both java deployable shapes (consumer + publish-only producer) compile under `gradle testClasses` on JDK 25.
**Slice 6c built:** backend fan-out — the Phoenix (Elixir) redis `broadcast`/`ephemeral` leg: `<App>.Channels.dispatch/2` tee module replacing the five `<Ctx>.Dispatcher.dispatch` call sites when channels are wired (Phoenix has no listener annotation to drop — routing lives in the tee: broker-routed events publish via Redix (MIT, §6a) and are never fanned out locally), `channel_consumer.ex` GenServer (Redix.PubSub → decode → local `<Ctx>.Dispatcher.dispatch`), foreign event struct modules + widened per-context subscription derivation, wiring-gated `redix` hex dep. Runtime-proven CROSS-BACKEND by the opt-in `channels-e2e-elixir` gate (`npm run test:channels-elixir`): the generated Hono producer publishes to valkey and the generated Phoenix consumer receives the same envelope, spawns the correlated Fulfil instance + Pending Shipment; both elixir shapes (consumer + tee-only producer) compile under `mix compile --warnings-as-errors`. Residual (in-mission): the ES-aggregate/ES-workflow/scheduler emit seams still dispatch locally-only on elixir — tee coverage for those paths rides the rabbit-leg slice.

**Slice 7a built:** rabbit fan-out — the Python (FastAPI) `queue`/`work` leg over the shared binding resolution: aio-pika driver (Apache 2.0, §6a) in `app/channels.py` with the §4 topology (durable fanout exchange per address, one durable queue per consuming deployable = `<address>.<deployable>` so replicas compete, manual ack → bounded `x-loom-attempts` retry → DLX `loom.dlx` → `loom.dlq.<address>` parking + `channel_dead_lettered` obs event), the §5 producer split (ephemeral events keep the inline tee — `publish_event` now routes ephemeral only; durable events land in `__loom_outbox` and publish on relay drain via `publish_event_from_relay` with the row id as the envelope id), a new workflow-less durable-producer dispatch shape (`ChannelTee → Outbox → Noop` + relay — the Hono forceOutbox twin), and the consumer-side `_current_event_id` idempotency marker where the consumer HOSTS the durable channel's context (foreign consumers keep the slice-3 ack-semantics stance — the stub-knob pin in `python/index.ts` documents why: module-level migrations can't see a foreign wiring). Wiring-gated per-transport deps (`aio-pika` iff rabbit, `redis` iff redis). Runtime-proven ALL-PYTHON by the opt-in `channels-e2e-rabbit-python` gate (`npm run test:channels-rabbit-python`): 6 events published by the generated FastAPI producer's outbox relay are consumed exactly once across 2 competing FastAPI replicas, and a poisoned message parks in the DLQ with the obs event; both shapes pass `ruff` + `mypy --strict`. Remaining fan-out: .NET/Java/Phoenix rabbit legs (the Phoenix one also carries the 6c residual — ES/scheduler emit-seam tee coverage), then kafka (slice 4).

**Slice 7b built:** rabbit fan-out — the .NET (ASP.NET Core) `queue`/`work` leg: RabbitMQ.Client driver (Apache 2.0, §6a — never MassTransit) in the generated `ChannelTransport.cs` with the §4 topology (durable fanout exchange per address, one durable queue per consuming deployable, `BasicQos(1)` + manual ack → bounded `x-loom-attempts` retry → DLX `loom.dlx` → `loom.dlq.<address>` parking + `channel_dead_lettered`), the §5 producer split (the tee's `Routing` table is now ephemeral-only; durable events pass to the outbox and publish on relay drain via the new `ChannelRelayPublisher` with the row id as the envelope id — the broker branch lands on BOTH persistence adapters' relays, efcore + dapper), the workflow-less durable-producer shape (outbox over the Noop + relay; `Program.cs` registers the Noop concretely), the consumer-side `OutboxDelivery.CurrentEventId` marker where the consumer hosts the durable channel's context (foreign consumers keep the slice-3 ack stance), and per-transport csproj refs (`RabbitMQ.Client` iff rabbit, `StackExchange.Redis` iff redis). Verified: 5 generator pins (`channels-rabbit-dotnet`) + the redis pins unchanged; five generated shapes (`{efcore,dapper} × {producer,consumer}` rabbit + redis regression) build under `dotnet build /warnaserror`; runtime-proven ALL-DOTNET in the SDK container (opt-in gate `npm run test:channels-rabbit-dotnet`): 6 producer emits ride the outbox relay to rabbitmq and are consumed exactly once across 2 competing replicas (1/5 split, same envelope id = outbox row GUID observed producer→consumer), and a poisoned message parks in the DLQ. Remaining fan-out: Java + Phoenix rabbit legs (the Phoenix one also carries the 6c residual), then kafka (slice 4).

**Slice 7c built:** rabbit fan-out — the Java (Spring Boot) `queue`/`work` leg, INCLUDING java's first transactional-outbox tier (java previously had none — events were publish-and-forget through Spring's ApplicationEventPublisher): `com.rabbitmq:amqp-client` driver (Apache 2.0, §6a — the plain-driver choice, consistent with Lettuce) with the §4 topology (durable fanout exchange per address, one durable queue per consuming deployable, `basicQos(1)` + manual ack → bounded `x-loom-attempts` retry → DLX `loom.dlx` → `loom.dlq.<address>` parking + `channel_dead_lettered`); the §5 producer split — the `ChannelPublishTee` @EventListener IS the outbox recorder on java: durable events `outbox.save(...)` inside the service's @Transactional write (atomic with the aggregate change) while ephemeral events publish inline; new `LoomOutboxMessage` JPA entity over the MigrationsIR-owned `__loom_outbox` + Spring Data repository + `OutboxRelayService` (SmartLifecycle poll loop) publishing drained rows via `ChannelRelayPublisher` with the row id as the envelope id; queue consumers dispatch ON the driver thread (strict path — the transport's retry/park owns failures) on the deployable's competing group; per-transport gradle deps (`amqp-client` iff rabbit, `lettuce-core` iff redis). **Residual (in-mission):** java saga `last_event_id` dedup is NOT wired (the migrations already emit the column; consumers rely on broker ack semantics + idempotent reactors — the slice-3 stance, one step behind python/dotnet's hosted-durable dedup). Verified: 5 generator pins (`channels-rabbit-java`) + redis pins unchanged; producer + consumer shapes build `gradle testClasses bootJar` on JDK 25; runtime-proven ALL-JAVA in the gradle container fleet (opt-in gate `npm run test:channels-rabbit-java`): 6 emits ride the outbox relay to rabbitmq, consumed exactly once across 2 competing replicas (2/4 split, same envelope id = outbox row UUID observed producer→consumer), poisoned message parks in the DLQ. Remaining fan-out: the Phoenix rabbit leg (also carries the 6c ES/scheduler tee residual), then kafka (slice 4).

**Slice 7d built:** rabbit fan-out — the Phoenix (Elixir) `queue`/`work` leg, INCLUDING elixir's first transactional-outbox tier AND the 6c emit-seam residual retired: hex `amqp` driver (MIT, §6a — same `~> 4.0` line the queue resource adapter pins) with the §4 topology (durable fanout exchange per address declared by the `ChannelBroker` publisher GenServer; consumer-side `Basic.qos(prefetch_count: 1)` + one durable queue per consuming deployable, manual ack → bounded `x-loom-attempts` header retry (republish + ack) → DLX `loom.dlx` → `loom.dlq.<address>` parking + `channel_dead_lettered`); the §5 producer split — `<App>.Channels.dispatch/2` now routes durable events to `record_durable/2` (a `LoomOutbox` Ecto insert joining the caller's `Repo.transaction`) while ephemeral events keep the inline publish, and the new `OutboxRelay` GenServer drains `__loom_outbox` and publishes via `publish_from_relay/3` with the row id as the envelope id (the outbox migration drops the bundled Ecto `timestamps()` — its time columns are explicit). The 6c residual seams (ES-aggregate append fan-out, ES-workflow post-append re-publish, every:/cron: scheduler ticks) now render through `elixirDispatchCall`, so broker-carried events tee from those paths too; workflow-handler `let` bindings nothing reads render `_name` (the previously-unused `bindName` pass), keeping wired consumers green under `--warnings-as-errors`. **Residual (in-mission):** elixir saga `last_event_id` dedup is NOT wired (same stance as java — broker ack semantics + idempotent reactors). Verified: 5 generator pins (`channels-rabbit-elixir`) + redis/6c pins unchanged; both shapes compile `mix compile --warnings-as-errors` in the hexpm container; runtime-proven ALL-ELIXIR in the container fleet (opt-in gate `npm run test:channels-rabbit-elixir`): 6 emits ride the outbox relay to rabbitmq (all publishes from `publish_from_relay`, zero undispatched rows), consumed exactly once across 2 competing replicas (4/2 split, same envelope id = outbox row UUID observed producer→consumer), poisoned message parks in the DLQ with `channel_dead_lettered`. Remaining: kafka (slice 4), then channel auth + k8s (slice 5).

**Slice 4 built:** Kafka — the log transport on the Hono reference driver: `SHIPPED_COMBOS.kafka = {broadcast/log, queue/work}`, kafkajs (MIT, §6a) driver in the generated `http/channels.ts` — one topic per channel address (idempotently admin-created by the first subscriber; a not-yet-produced topic otherwise throws `UNKNOWN_TOPIC_OR_PARTITION` and would crash boot), partition key = **`loomkey` ?? envelope id** (the channel's `key:` field value now rides the envelope as `loomkey` — the `key:` knob's first consumer), consumption ALWAYS on the deployable's consumer group (per-deployable groups realise broadcast ACROSS deployables and competition WITHIN one — the §4 kafka row), offsets commit after the handler resolves, dead-letter v1 = log + park onto `<address>.dlq` (a failed/malformed record parks and the partition keeps moving — never a hot-loop). `log` retention is durable, so the §5 producer split applies unchanged: tee → outbox → relay publishes with the row id. Compose provisions the official `apache/kafka:4.1.0` (Apache 2.0, KRaft single node — **never bitnami**, §6a) with in-network advertised listener + `KAFKA_NUM_PARTITIONS: 3` (so replica groups actually share; ordering rides the key, not the count) and injects `LOOM_CHANNEL_<NAME>_URL: "<svc>:9092"`. Wiring-gated `kafkajs` dep; redis/rabbit emissions stay byte-identical (regression-pinned). Verified: 4 generator pin suites green (kafka + redis + rabbit regressions); both generated shapes `tsc --noEmit` clean; runtime-proven by the opt-in `channels-e2e-kafka` gate (`npm run test:channels-kafka`), GREEN in-sandbox: 12 emits (2 per order, 6 orders) ride the outbox relay onto the topic, consumed exactly once across 2 competing replicas, **ordering-per-key held** (both of each order's events landed on the SAME replica, `OrderPlaced` strictly before `OrderShipped`), and a poisoned record parked on `<address>.dlq` with `channel_dead_lettered`. Remaining: per-backend kafka fan-out (python/dotnet/java/elixir), then channel auth + k8s (slice 5); the M-T4.2 replay-cursor hook stays future.

**Slice 8a built:** kafka fan-out — the Python (FastAPI) leg: aiokafka (Apache 2.0, the asyncio-native client — the async twin of the design table's confluent-kafka pick) `KafkaChannelTransport` in `app/channels.py` with the §4 topology (topic per address idempotently admin-created before the group join — 3 partitions/rf 1 matching the compose sidecar; partition key = `loomkey` ?? envelope id, the envelope stamping the channel's `key:` field value; consumption always on the deployable's group; `enable_auto_commit=False` + commit after the handler resolves; dead-letter v1 park onto `<address>.dlq`), the §5 split unchanged (`log` durable → outbox → `publish_event_from_relay` with the row id), the wiring-gated `aiokafka` dep + a scoped mypy override (aiokafka ships no py.typed). Two cross-cutting fixes ride along: (1) **carried-but-unsubscribed events now join the consumer's codec/vocabulary** (python previously only resolved SUBSCRIBED foreign events, so a carried type with no reactor raised "unknown carried event type" and wrongly dead-lettered — transport-agnostic bugfix); (2) the pure-producer outbox shape composes with #2034's realtime tee (broadcast+durable was impossible pre-kafka; the `OutboxDispatcher` inner is typed for the wrap). Verified: 4 generator pins (`channels-kafka-python`) + rabbit/redis python pins unchanged; both shapes pass `ruff` + `mypy --strict` + `compileall`; runtime-proven ALL-PYTHON by the opt-in `channels-e2e-kafka-python` gate, green in-sandbox: 12 emits ride the outbox relay onto the topic, exactly-once across 2 competing replicas, ordering-per-key held (each order's two events on the SAME replica, in emit order), poisoned record parked on `<address>.dlq`. Remaining kafka fan-out: dotnet/java/elixir.

**Slice 8b built:** kafka fan-out — the .NET (ASP.NET Core) leg: `KafkaChannelTransport` (Confluent.Kafka 2.6.1, Apache 2.0 — the §6a pick) in the generated `ChannelTransport.cs` with the §4 topology (topic per address idempotently admin-created before the group join — 3 partitions/rf 1 matching the compose sidecar; partition key = `loomkey` ?? envelope id, the envelope gaining a `WhenWritingNull` `loomkey` stamped from the channel's `key:` field via the binding row; consumption always on the deployable's group — kafka bindings take the strict subscribe path; `EnableAutoCommit=false` + commit after the handler resolves; dead-letter v1 park onto `<address>.dlq`; per-subscription poll loop on a background task, canceled on close), the §5 split unchanged (`log` durable → outbox → relay with the row id), the consumed log carrying the partition key, and the wiring-gated `Confluent.Kafka` csproj ref. The 8a carried-but-unsubscribed codec fix is applied for parity (dotnet previously resolved only SUBSCRIBED foreign events — a carried type with no reactor threw "unknown carried event type" and wrongly dead-lettered; caught live by the runtime proof). Verified: 3 generator pins (`channels-kafka-dotnet`) + rabbit/redis dotnet pins unchanged; both shapes `dotnet build /warnaserror` clean in the SDK container; runtime-proven ALL-DOTNET in the SDK-container fleet (opt-in gate `npm run test:channels-kafka-dotnet`): 12 emits ride the outbox relay onto the topic, consumed exactly once across 2 competing replicas (8/4 split, zero dead-letters), ordering-per-key held (all 6 keys single-owner, `OrderPlaced` before `OrderShipped`), poisoned record parked on `<address>.dlq` with `channel_dead_lettered`. Remaining kafka fan-out: java/elixir.

**Slice 8c built:** kafka fan-out — the Java (Spring Boot) leg: `KafkaChannelTransport` (plain `org.apache.kafka:kafka-clients` 3.9.1, Apache 2.0 — the Lettuce/amqp-client plain-driver choice) with the §4 topology (topic per address idempotently admin-created before the group join — 3 partitions/rf 1 matching the compose sidecar; partition key = `loomKey` ?? envelope id, the envelope + binding gaining the channel's `key:` field; consumption always on the deployable's group via the strict driver-thread path; `enable.auto.commit=false` + `commitSync` after the batch's handlers resolve; dead-letter v1 park onto `<address>.dlq`; daemon poll-loop thread per subscription, woken on close), the §5 split unchanged (`log` durable → outbox → relay with the row id), the consumed log carrying the partition key, and the wiring-gated `kafka-clients` gradle dep. NOTE: the 8a/8b carried-but-unsubscribed dead-letter bug does NOT bite java — its dispatch switch decodes per-arm with a no-op default, so an unhandled carried type never reaches the codec. Verified: 3 generator pins (`channels-kafka-java`) + rabbit/redis java pins unchanged; both shapes `gradle testClasses bootJar` clean in the gradle:9-jdk25 container; runtime-proven ALL-JAVA in the container fleet (opt-in gate `npm run test:channels-kafka-java`): 12 emits ride the outbox relay onto the topic, consumed exactly once across 2 competing replicas (6/6 split, zero dead-letters), ordering-per-key held (all 6 keys single-owner, `OrderPlaced` before `OrderShipped`), poisoned record parked on `<address>.dlq` with `channel_dead_lettered`. Remaining kafka fan-out: elixir.

**Slice 8d built:** kafka fan-out — the Phoenix (Elixir) leg, **completing the broker × backend matrix** (redis/rabbit/kafka × hono/python/dotnet/java/elixir): brod `~> 4.4` (Apache 2.0, Klarna's plain Erlang client — the Redix/amqp plain-driver choice) with the §4 topology — `KafkaBroker` GenServer publisher (one `:brod` client per broker URL, topic per address idempotently admin-created before first publish — 3 partitions/rf 1 matching the compose sidecar; `produce_sync` through the `:hash` partitioner with partition key = `loomkey` ?? envelope id, the envelope stamping the channel's `key:` field via a new `@channel_keys` map; the transmit spine goes 5-arity only when kafka is wired — redis/rabbit stay byte-identical), a `brod_group_subscriber_v2` `KafkaConsumer` on the deployable's group (`<address>.<deployable>`; topic ensured before the group join; offsets `commit_to_kafka_v2` after the handler; dead-letter v1 park onto `<address>.dlq` with a create-topics + bounded-retry fallback — the FIRST park races the dlq topic's creation/metadata propagation), and the §5 split unchanged (`log` durable → 7d outbox → relay with the row id). **Three brod runtime findings are now emitted config, caught live by the fleet:** (1) `message_type: :message` — the message_set default hands `handle_message/2` a batch record, not a `kafka_message`; (2) `group_instance_id: :null` — brod DEFAULTS to a static group instance id derived from `node()`/pid, so unnamed nodes (`nonode@nohost`) collide across replicas and fence each other out of the group in an `:unknown_member_id` rejoin ping-pong (double consumption, commits never land); (3) brod pulls the `crc32cer` C NIF, so the generated Dockerfile's builder stage gains `cmake` iff kafka is wired. Verified: 6 generator pins (`channels-kafka-elixir`, incl. rabbit-regression stability) + rabbit/redis elixir pins unchanged (full elixir subtree green); both shapes `mix compile --warnings-as-errors` clean in the hexpm container; runtime-proven ALL-ELIXIR in the container fleet (opt-in gate `npm run test:channels-kafka-elixir`): 12 emits ride the outbox relay onto the topic, consumed exactly once across 2 competing replicas (10/2 split, zero dead-letters), ordering-per-key held (all 6 keys single-owner, `OrderPlaced` before `OrderShipped`), poisoned record parked on `<address>.dlq` with `channel_dead_lettered`. Remaining in the mission: channel auth + k8s subchart + docs promotion (slice 5); the M-T4.2 replay-cursor hook stays future.

**Slice 5a built:** broker service-to-service auth (§7) — the v1 stance shipped on all three brokers, credentials riding the one seam every driver already consumes (the `LOOM_CHANNEL_<NAME>_URL` env; production overrides the URL, no second credential channel): **redis** `requirepass` on the Valkey sidecar (single credential v1, `redis://:<pass>@…`; the .NET driver gained the URL-password → SE.Redis `password=` parse — every other driver's client takes the credentialed URL natively); **rabbitmq** one vhost `loom` + one user per deployable with configure/write/read scoped to its compiler-known exchanges/queues, provisioned by a generated + mounted `broker-init/<slug>-definitions.json` (`load_definitions`, which also suppresses the image's default open `guest` account; salted-SHA-256 hashes from a new browser-safe `src/util/sha256.ts` — `src/system/` can't use node:crypto); **kafka** SASL/PLAIN on the CLIENT listener (`kafka://user:pass@…`, one JAAS `user_<name>` per wired deployable; inter-broker + healthcheck stay on a loopback PLAINTEXT listener; topic ACLs deferred) with all five drivers parsing the URL userinfo into their SASL config (kafkajs `sasl:`, aiokafka `sasl_plain_*`, Confluent.Kafka `ApplySasl`, kafka-clients `sasl.jaas.config`, brod `sasl: {:plain, …}`). Dev credentials are deterministic (`loom-dev-<storage>[-<user>]` — the compose `POSTGRES_PASSWORD: postgres` stance); a credential-LESS URL keeps every driver on the plain pre-auth contract, so the native e2e harnesses stay green unchanged. Rode along: `KafkaChannelTransport` (.NET) now implements `IDisposable` — CA1001 started failing `/warnaserror` on the current `dotnet/sdk:10.0` image against ALREADY-MERGED 8b output (SDK analyzer drift), fixed here. Verified: 9 pins (`channels-auth`, incl. per-backend parse + scoped-ACL regex + hash vectors) + all broker transport pins updated to the authed compose; all five backends strict-compile the changed drivers (tsc / ruff+mypy / `/warnaserror` / gradle / `--warnings-as-errors`); runtime-proven by the new opt-in `channels-e2e-auth` gate (`npm run test:channels-auth`) — ONE generated system wiring all three brokers at once (also the first runtime proof of the multi-transport factory), each broker container provisioned from the GENERATED artifacts (requirepass command line, definitions file, JAAS line), one event per authed broker delivered across deployables, and credential-less access refused per broker (NOAUTH / no-guest / SASL-less client rejected). Remaining in the mission: k8s broker subchart (5b) + `docs/channels.md` promotion (5c).

**Slice 5b built:** k8s broker subchart — the chart-side sibling of the compose broker sidecars (design §4 compose note; auth per §7): `buildBrokers` joins the shared `WorkloadModel` seam (`src/system/kubernetes.ts`), and each wired broker storage gets an **enabled-gated** workload in the chart (`helm/templates/<name>-broker.yaml` — Deployment + ClusterIP Service + a ConfigMap for RabbitMQ's mounted `load_definitions` file; `.Values.brokers.<storage>.enabled`, default true) with the same §7 auth provisioning as compose (valkey `--requirepass`; rabbit `loom` vhost + per-deployable scoped users, `guest` suppressed; kafka KRaft SASL/PLAIN client listener with per-deployable JAAS users, advertised at the release-prefixed broker Service via `printf`/fullname). Every wired deployable's `LOOM_CHANNEL_<NAME>_URL` rides the shared chart `Secret` (it embeds credentials — secret-classified by construction, not name-guessing) with an in-cluster default computed in the Secret template and a per-deployable `.Values.<deployable>.channels.<ENV>` override for managed brokers; the raw `k8s/` view mirrors everything with defaults baked (plain service names). Channel-less systems emit no broker artifacts (byte-identical; pinned). Verified: 5 new pins in `kubernetes-helm.test.ts` (gating, SASL/JAAS + advertised-listener templating, Secret default + deployment env ref, rabbit definitions ConfigMap, channel-less absence) — full `test/system` subtree green; `helm lint` + `helm template` pass on a generated three-broker system with the rendered chart resolving release-prefixed URLs/advertised listeners and the raw view plain names (kubeconform rides the existing `k8s-build.yml` gate); `docs/kubernetes.md` gains the broker-channels section. Remaining in the mission: `docs/channels.md` promotion (5c).

**Slice 5c built:** `docs/channels.md` promoted from the proposal to a shipped-feature reference doc — grammar surface (`channel`/`channelSource`/`channels:`) with a runnable two-deployable example + generated compose excerpt, the shipped delivery/retention × transport matrix (NATS stays a parseable-but-rejected storage type), the 5-backend × 3-broker driver/license table, the CloudEvents envelope + delivery-uniformity rule + §5 outbox split, per-broker topology (§4), the §7 auth stance, the 5b chart wiring, the `test:channels*` gate matrix, and the not-yet list (M-T4.2 replay cursor, ACLs beyond v1, M-T1.10 browser delivery, java/elixir saga `last_event_id` dedup). Linked from `docs/README.md` + CLAUDE.md further-reading. **This completes the mission's slice plan** (design §8 rows 1-6+ all shipped: clause/validators/envelope → redis → rabbit → kafka → auth + k8s + docs → full backend fan-out); the M-T4.2 replay-cursor hook stays future.

Sources: [channels.md](../old/proposals/channels.md) §brokers, production-readiness §3.3, weak-spots (runtime islands).

## M-T4.5 — Saga hardening slices (in-flight review remediation) — `in-flight` · **S–M** · P1
Live branches from the generated-code DDD review: S5(a) Phoenix persist-then-dispatch (+S12), S5(b) ES saga starter exists-guard (all 5 backends), S5(c) Java unconditional publisher, Phoenix op guards 403/422. Land or re-drive each; they're small and correctness-grade.
Sources: [phoenix-event-delivery-s5a](../old/plans/phoenix-event-delivery-s5a.md), [saga-starter-guard-s5b](../old/plans/saga-starter-guard-s5b.md), [java-uniform-publisher-s5c](../old/plans/java-uniform-publisher-s5c.md), [phoenix-op-guards-403-422](../old/plans/phoenix-op-guards-403-422.md).

## M-T4.6 — Day-one batteries: `job`, `email`, object `storage` — `partial` · **L** · P1
The ~100%-of-apps integrations: an email adapter (resource kind — smtp/ses/sendgrid), object-storage `File`/`Upload` surface, and `job` (folds into M-T4.1 timers). **Object-storage `File` slice 1 shipped** (PR #2007, tracked under M-T1.2): the `File` field type + `FileRef` wire contract on all five backends + a dependency-free `localDisk` store + Hono `POST /files`/`GET /files/:key` endpoints. (The `files.put/get/signedUrl` workflow-body verbs already rendered; this adds the declarative field type they hang off.)
**Email landed (foundation + all-five-backend emission):** surface `kind: mailer` → infra kind `email`; sourceTypes smtp/ses/sendgrid; one `mail.send(to, subject, body)` verb consumed from workflows. Grammar/IR/registry/verb-table + ResourceAdapters on hono (nodemailer), .NET (MailKit), java (Jakarta Mail), python (aiosmtplib), phoenix (Swoosh) — each with SES/SendGrid siblings — plus a Mailpit dev compose sidecar. Call-site rendering was already verb-name-generic (no per-backend change). Unit-gated (registry/lowering/negative-verb/cross-backend emission/compose). **4.6-email-b landing** (PRs #2051/#2059/#2065): the emitted vendor clients are compile-gated per backend via the corpus/elixir-vanilla build fixtures (with a `<NuGetAuditSuppress>` for MailKit's transitive NU1902), and a `LOOM_EMAIL_E2E` **Mailpit** runtime leg — fanned across all five backends — boots the generated backend and asserts `mail.send(...)` delivers into a Mailpit inbox. **Remaining:** templated/HTML email (4.6-email-c, own proposal), `job`, and the File follow-ons (other-backend endpoints, s3 presign, the `FileUpload` UI primitive) tracked under M-T1.2.
Sources: [quickstart-and-day-one-batteries](../old/proposals/quickstart-and-day-one-batteries.md) §5, completeness-audit Tier 1; email slice plan: [email-resource-kind](../old/proposals/email-resource-kind.md).

## M-T4.7 — Workflow family v2 — `open` · **L** · P3
Deferred reframes: workflow-as-aggregate `on(...)` handler surface; snapshots (aggregate + saga state); the sagas compensation contract; `repo-let` arrays/nullables (gated); ES workflow instance *pages* (list/detail — the one open slice from workflow-debt parity).
Sources: [workflow-and-applier](../old/proposals/workflow-and-applier.md), [workflow-debt-backend-parity](../old/plans/workflow-debt-backend-parity.md) next-slice, global-plan T3.5/T3.6.

## M-T4.8 — Resource-model completion — `partial` · **M** · P3
Remaining `sourceType`/`interface` registry entries + vendor emission beyond the shipped verb set; broader `need ⊆ sourceType` activation; the `contract` typed-resource declaration (inbound `from openapi(...)` typed clients) as the follow-on.
Sources: [resource-model-and-source-types](../old/proposals/resource-model-and-source-types.md), [workflow-resource-consumption](../old/proposals/workflow-resource-consumption.md), [contract-typed-resources](../old/proposals/contract-typed-resources.md).

## M-T4.9 — Read caching tier — `open` · **L** · P3 (proposal needed)
`cached(ttl)` + `CacheAdapter` + invalidation keyed by the channel/resource vocabulary (channels Part II — entirely unstarted).
Sources: [channels.md](../old/proposals/channels.md) Part II, production-readiness §3.4.

## M-T4.10 — Backend-to-backend calls — `open` · **L** · P3 (proposal needed)
Typed inter-deployable invocation (peer-URL IR, client emission, authn between services). Referenced by production-readiness §3.10 and deployable-networking's out-of-scope note; no owning proposal yet — write it.
