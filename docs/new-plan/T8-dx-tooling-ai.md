# T8 — DX, tooling & the AI platform

> **Completed missions for this track live in [`archive/T8-done.md`](archive/T8-done.md)** (2 closed as of 2026-09-02). This file lists only the live missions.

*Diagnostics (123 stable codes + machine-applyable fix-hints) and the LSP are best-in-class; interactive debugging is essentially absent; the AI-platform loop (validate→repair→verify) is mostly built and needs its last mile + the wedge demo.*

## M-T8.1 — Delegating DAP debugger — `open` · **XL** · P2
The shipped DAP server is a remap shim (breakpoint/stack-trace translation only; no stepping/variables; VS Code has no `debuggers` contribution). Build the delegating proxy: spawn js-debug (node first), forward launch/attach/continue/stepIn, remap both directions; register the debugger in the extension; the manual VS Code breakpoint confirmation. Then coreclr/JDWP per backend.
Sources: [source-map-and-debugging](../old/proposals/source-map-and-debugging.md) §6E, [dap-node-debug](../old/plans/dap-node-debug.md) frontier list.

## M-T8.2 — Sourcemap fan-out — `partial` · **M** · P3
Column-accurate span tracking shipped on TS/Hono (slices 3–4 incl. `ddd trace` columns); fan out `renderExprWithMarks` to the other four backends when a consumer exists (DAP or per-backend crash remap). Deliberately deferred — re-check the consumer question first.
Sources: [span-tracking-emission](../old/plans/span-tracking-emission.md), [source-map-debug-kickoff](../old/plans/source-map-debug-kickoff.md).

## M-T8.3 — Agent-loop last mile: chat UI + context pack + wedge demo — `partial` · **L** · P1
The strategic demo: prose → `.ddd` → multi-backend generate → conformance green, driven by an agent in the playground. **Remaining (design-captured, not yet built):** the full **model context-pack** (grammar cheatsheet + worked examples beyond the seed system prompt; gate: an eval harness measuring frontier-model zero-shot validity) — proposal [`model-context-pack.md`](../old/proposals/model-context-pack.md); and the **`loom_verify`** tool (per-requirement Definition-of-Done verdicts), gated on the M-T8.6 sandbox test-runner — proposal [`loom-verify-tool.md`](../old/proposals/loom-verify-tool.md). **In flight (PRs):** token-level STREAMING (#2096), the next fix-hint batch — `token-nullable` + `{react,svelte,vue,angular}-deployable-missing-ui`, which also enabled `add`-on-deployable (#2122), and the `rename` model-patch op — declaration + all cross-references (#2125).
- **Shipped (agent tools):** the `loom_read_model` (resolved wire-shape projection over the enriched IR — the semantic contract `loom_outline` omits) and `loom_list_primitives` (the closed walker page-body vocabulary) agent tools — both over `src/api/`, browser-safe, in the shared catalog (MCP + playground). `loom_verify` still open (it joins test-results, so it's coupled to the runtime/chat loop — see [`loom-verify-tool.md`](../old/proposals/loom-verify-tool.md), gated on the M-T8.6 sandbox runner).
- **Shipped (evolution tools):** `loom_snapshot` (`{source}→SnapshotReport` — the `ddd snapshot` provenance capture returned as data, no file emission) and `loom_diff` (`{source,baseline?}→DiffReport` — the schema-migration + wire-contract delta a change implies, with the destructive/breaking gate surfaced), both in `src/api/evolve.ts` over the SAME shipped pure cores the human Migrations dock rides (`captureSnapshots`/`buildMigrations`/`diffWireSpec`). This is the M-T8.11 "an agent editing a model wants the same evolution signals" tie-in — the agent now gets the migration/breaking-change signals a human sees in the dock. (`loom_verify` remains the one deferred evolution-adjacent tool, per above.)
- **Shipped (deterministic demo):** the **Agent dock tab** (`web/src/layout/ChatPanel.tsx`) plays a SCRIPTED wedge demo — prose → authored `.ddd` (into the editor) → `loom_validate` → `loom_generate` (real browser-safe `callTool`) → real playground generate → green — with no live LLM, so it's reproducible and doubles as a Playwright e2e (`web/e2e/agent-demo.spec.ts`). Driver + authored source are headless-gated (`test/playground/agent-demo.test.ts` validates + generates the model). This decouples the wedge demo from the still-open `Complete` transport/provider decision (the LIVE variant).
- **Shipped (BYOK transport):** the concrete `Complete` behind the live loop — a generic **OpenAI-`/chat/completions`-compatible** adapter (`web/src/agent/openai-transport.ts`) that converts the loop's Anthropic-shaped `Message`/`ToolSpec`/`Completion` to/from the OpenAI wire shape, so ONE code path reaches OpenRouter (default), OpenAI, Groq, or a local llama.cpp — new providers are presets (`web/src/agent/provider.ts`, `ProviderKind` reserves the `proxy`/`anthropic` seams), not code. BYOK: the key lives only in browser localStorage (`AgentSettings`), sent as a bearer to the chosen provider. A seed system prompt (`system-prompt.ts`) lists the live tool inventory. Headless-tested with an injected `fetch` driving a real `loom_validate` through `runAgent` (`test/playground/agent-transport.test.ts`).
- **Shipped (live chat UI):** the Agent dock tab is now a real **live chat** on top of that transport — a composer (Enter-to-send) + a BYOK settings popover (provider preset select, base URL, model, password key) alongside the key-free demo button. `web/src/agent/live.ts` is the browser-safe engine: `foldTranscript` (raw `Message[]` → display bubbles + tool-call cards), `summarizeToolResult`, `latestSourceFrom` (the agent's newest `.ddd` is reflected into the editor as it works), and `runLiveAgent` (one turn over the carried transcript, then a real generate). Persisted `AgentSettings` + `sendAgentMessage`/`clearAgentChat` live in `App.tsx`/`ctx.ts`; a `__loomAgentComplete` seam injects a scripted transport so the whole live path is e2e-testable without a key (`web/e2e/agent-live.spec.ts`). 9 headless tests (`test/playground/agent-live.test.ts`).
Sources: [ai-authoring-loop](../old/proposals/ai-authoring-loop.md) items 7+9, [agent-tools-and-mcp](../old/proposals/agent-tools-and-mcp.md) §8-5, [ai-generation-platform](../old/proposals/ai-generation-platform.md) §6, [model-context-pack](../old/proposals/model-context-pack.md), [loom-verify-tool](../old/proposals/loom-verify-tool.md), D-AI-EMPHASIS, D-AGENT-TOOLS.

## M-T8.4 — LSP correctness tail — `open` · **S** · P2
The LValue blind spot (rename-from-call-site, member-call highlighting in statement position), uninferrable-receiver rename miss; the `Fold to macro` inverse code action.
Sources: [agent-tools-and-mcp](../old/proposals/agent-tools-and-mcp.md) §4c.

## M-T8.5 — Diagnostics contract completion — `partial` · **M** · P3
`related[]`, IR-diagnostic ranges + fixHints (needs CST provenance through lowering), GenerateReport file counts + `.loom` paths, multi-file generate, code-registry single-sourcing, `contractVersion`.
- **Shipped (fix-hints reach the editor):** the repair surface was two disconnected halves — 8 of 419 codes carried a `fixHint` for the agent loop, and the editor hand-rolled ONE of them plus a quick fix for `loom.framework-mismatch`, a code no validator emits (dead through every green run; the docs advertised it). `DddCodeActionProvider` now delegates to the `fix-hints.ts` registry through `resolvePatchEdits`, and the playground's Monaco registers the same actions over the already-shipped-but-uncalled `fixHintCodeActions` — so a new provider is a one-entry change that lands on all three surfaces at once. `choose`-kind hints (several valid repairs, e.g. which `ui` to bind) fan out one action per option, none preferred. Providers **8 → 14 codes**, the notable one being `loom.unknown-name`: the validator already computed the did-you-mean candidate and discarded it into prose, so the repair existed with no way to reach it — it now travels as structured diagnostic `data`, which is the mechanism the next batch rides. Gated by `test/language/fix-hint-coverage.test.ts` (a provider or code-action arm naming a code the catalog does not know FAILS — the dead-quick-fix class — plus a coverage floor).
- **Shipped (the address space, and the round trip that pins it):** the reach limit named below is gone. `addressOf` hardcoded TWO qualifier slots (one bounded context, one aggregate/value object) and mis-addressed — not merely failed to address — everything else: the whole `ui` subtree carried NO qualifier (two pages named alike in different uis were one address), an entity part's fields collapsed onto the aggregate's own address, event/payload/capability fields had no keyword (`node Sales.at`), and two `apply(...)` shared one address. The resolving half, `indexTargets`, kept a separate hand-maintained container list that had DRIFTED — it looked for pages under `BoundedContext.members`, which the grammar never allows, so page addresses resolved to nothing and `outline.contexts[].pages` was permanently empty. Both halves now derive from one rule: `addressOf` walks the container chain (`page Admin.Back.Board`, `entity Sales.Order.Line.qty`), the index is one walk over the same `isAddressable` predicate, `apply` is keyed by the event it folds, and a payload keeps its authored spelling. `OutlineContext.pages` → `OutlineSystem.uis`. Gated by `test/language/address-round-trip.test.ts` (resolves-to-own-node / no collisions / no placeholder keyword), which fails **77 / 1 / 50** on the pre-fix commit. First ui-scoped repair shipped on top (`loom.bindable-input-value-arg`), and `loom.unknown-name` reaches value-object members. Correction to the note below: ui diagnostics were said to be offerable editor-side once the address space grew — for `unknown-name` that is wrong, since `Ui` sits in that validator's `SKIP_CONTAINERS` where bare identifiers are deliberately unresolved.
- **Open (next batch):** the ~38 remaining catalog messages that state a mechanical repair — now a matter of writing providers, not of reaching the nodes. `loom.cross-aggregate-entity-part` was investigated and is unreachable (the scope provider filters foreign entity parts out of every bare-name type position).
Sources: [ai-diagnostics-contract](../old/proposals/ai-diagnostics-contract.md).

## M-T8.6 — Playground sandbox completion — `partial` · **M** · P2
The cross-origin flip (`SANDBOX_ORIGIN` → distinct origin — no real isolation until then; gates untrusted user expressions), Phase 3 API test runner, Phase 4 UI driver + `page` shim, Phase 5 console/screenshots, CSP pack-render confirmation.
Sources: [playground-sandbox-redesign](../old/plans/playground-sandbox-redesign.md).

**Gate-health note (2026-08-10, #2445):** the Playground-e2e gate had been red on `main` since Aug 4 — two same-shaped races on shared mutable refs written outside the guard protecting the state they must agree with (`lastBundleReadyRef`: the Bundle button was live ~700ms before it did anything; `lastMappedGenerateRef`: overlapping cycles clobbered each other's sourcemaps — fixed by deleting the shared slot). New per-PR gate `e2e/bundle-starts.spec.ts`. Second instance of the "red-on-main-for-days, alarm not listening" pattern in one week (with #2434) — the alarm half is now `ci-red-alarm.yml`; the flake half is M-T9.30.

## M-T8.7 — Packaging split unblock — `blocked(browser discovery)` · **L** · P3
P3-s5 (move `src/platform/hono/v*` into `packages/`) is blocked on browser-capable backend discovery for the playground worker (`ResolutionStrategy` seam, esbuild-wasm spike → RegistryStrategy → WorkspaceStrategy). P4 publish follows. Related: [per-package-output-tree](../old/proposals/per-package-output-tree.md) (output-side twin, deferred), [server-side-generation](../old/proposals/server-side-generation.md) (fills the same worker seam with a server call — evaluate together).
Sources: [packaging-split](../old/plans/packaging-split.md), [backend-packages](../old/plans/backend-packages.md) B3+.

## M-T8.8 — Mutation testing — `open` · **XL** · P3 (explicitly parked)
IR-level mutation testing (mutate `ExprIR`, render via the shared dispatcher, kill/survive against emitted suites → `VERIFIED_WEAK` verdicts in `ddd verify`). The old global plan marked it out-of-scope; the proposal is complete. Revisit after T9's runtime tiers mature.
Sources: [mutation-testing](../old/proposals/mutation-testing.md).

## M-T8.9 — Static-analysis breadth — `open` · **S** · P3
markdownlint + biome JSON/JSONC extension; Credo; `ddd fmt` stays a separate future proposal.
Sources: [static-analysis-followups](../old/plans/…) — see [cross-stack-static-analysis](../old/proposals/cross-stack-static-analysis.md).

## M-T8.10 — Playground preview breadth — `open` · **M** · P3
In-browser preview boots Hono+React only; Vue/Svelte previews need their compilers in the VFS bundler; multi-backend mounting ties to M-T7.4 slice 4. Nice-to-have; the builder already edits all frontends' source.
Sources: [vue-frontend-plan](../old/plans/vue-frontend-plan.md)/[svelte-frontend-plan](../old/plans/svelte-frontend-plan.md) deferrals, [playground.md](../playground.md).

## M-T8.11 — Playground evolution-lifecycle surfacing — `partial` · **M** · P2
The playground regenerates statelessly (keyed by a source hash) and holds no "previous version of my system", so migrations/snapshots/wire-contract *changes* are invisible side effects — the git-VFS versions source, but nothing diffs derived artifacts against a pinned baseline. Three deliverables, all riding shipped pure cores: a **snapshot capture button + `.loom/snapshots` browser** (`captureSnapshots` is already imported by the build worker; the `snapshot` RPC + client method exist but no UI calls them); a **Migrations & Contract dock tab** driven by a pinned git baseline through `memorySnapshotStore` + `diffSchema`/`buildMigrations` (surfacing the destructive gate + `MigrationDestructiveError` instead of today's read-only "schema migrated" badge); and a **breaking-change panel** over the new pure `diffWireSpec` (replacing eyeball `git diff .loom/wire-spec.json`). The **baseline concept is the crux** — every diff needs a pinned prior version, which the git workspace can supply. Web-side counterpart to the T2 data-evolution track: `checkMigrationBaseline` (M-T2.2) shipped fs-backed/CLI-only and the playground deliberately omits it. Ties to M-T8.6 (real sandbox isolation) and M-T8.3 (agent loop — an agent editing a model wants the same evolution signals).
- **Shipped (#2017):** all three deliverables — the Migrations dock tab (`web/src/layout/MigrationsPanel.tsx`), the `evolution` RPC + `diffWireSpec` breaking-change core, the snapshot capture button, a git-baseline picker, and a Download-.zip export of the generated tree.
- **Shipped (#2048):** the noted follow-ups — **multi-file / import baselines** (both trees now seed the worker VFS and lower via the project loader, so `import` graphs resolve; the single-file gate is gone), a **mobile Download-.zip** button, a **History → "Diff as baseline"** one-click pin (dock-tab state lifted to the ctx), and an edit-driven + multi-file Migrations e2e.
- **Shipped (mobile parity):** the Migrations dock tab + the Agent chat tab, previously **desktop-only** (they lived in `DevToolsDock`, which the mobile shell has no equivalent of), are now bottom-nav tabs in `web/src/layout/MobileShell.tsx` — `MigrationsBody`/`ChatBody` are `ctx`-driven, so the evolution surface and the agent are reachable on a phone (visibility-gated async git reads, matching History/Tests). Remaining under this mission: nothing structural — the evolution-signals-**for-the-agent-loop** tie-in shipped under M-T8.3 (`loom_diff`/`loom_snapshot`). The Migrations tab's *presentation* — the "Compare with" copy, the diff-tinted schema diagram beside the SQL, the destructive gate rendered as `--allow-destructive` + the data it would drop, and the baseline list's failure branch — was reworked under **M-T8.22** (PR #2762), which extends this mission's surface rather than replacing its cores (`runEvolution` / `diffWireSpec` / the snapshot RPC are unchanged; the worker DTO gained `tables` + a per-step `table`).
Sources: [playground.md](../playground.md), [source-map-and-debugging](../old/proposals/source-map-and-debugging.md) (provenance/`.loom` bundle), T2 track ([T2-data-evolution](T2-data-evolution.md) M-T2.1/M-T2.2/M-T2.3).

## M-T8.14 — Playground crash reporting & diagnostics surfacing — `partial` · **M** · P2
Both error boundaries (root + the per-pane one from #2287) already push every crash class into the 12-entry `loom.diag` ring, but `logDiagnostic` takes only a *reason* — the error message, stack and component stack go to `console.error` and die with the tab, so a user crash report is unfalsifiable even when the user cooperates. Three no-infrastructure slices: capture completeness (a `detail` argument on the ring + build SHA via a vite `define` + a `worker.onerror` class for the currently-unobservable build-worker death + a `lastCrash` flag), a report surface (redacted "Copy crash report" / prefilled `crash-report` GitHub issue form on both fallbacks + a next-boot notice; the BYOK key `localStorage["loom.agent.settings"].apiKey` must never enter a report), and the team-side triage shell (the repo has no `.github/ISSUE_TEMPLATE/` at all + a `docs/playground.md` diagnostics section). Slice 4 (opt-in beacon) is enumerated and **recommended deferred** — decision-gated on report volume. P2: not user-facing capability, but it gates the feedback loop for every crash class the audit could not falsify.
**Slices 1–3 shipped.** `logDiagnostic(reason, detail)` now carries message/stack/componentStack/pane, every entry is stamped with a build SHA (vite `define` ← `GITHUB_SHA`), the build worker has an `error`/`messageerror` class (`worker-error`), and an error-class capture sets a `loom.diag.lastCrash` flag a boot-time dismissible notice reads. `web/src/util/crash-report.ts` assembles a deterministic redacted markdown report (storage-blind by construction, so the BYOK key cannot reach it) behind **Copy crash report** / **Report on GitHub** on both fallbacks, the notice, and Output → Diagnostics; `.github/ISSUE_TEMPLATE/crash-report.yml` (+ `config.yml`) receives it prefilled by field id. `?crash=app|pane` is the production-safe boundary trigger the new `web/e2e/crash-reporting.spec.ts` drives (per-PR no-network lane). `partial`, not `done`, only because slice 4 (opt-in anonymous beacon) stays deliberately **deferred** — revisit per the design's falsifiable criterion.
Design: [M-T8.14-crash-reporting-design](missions/M-T8.14-crash-reporting-design.md). Sources: [playground-file-mgmt-review-2026-07](../audits/playground-file-mgmt-review-2026-07.md) §3.3.

## M-T8.15 — Mobile light: shed the IDE, keep the app — `partial` · **L** · P1
The playground does not boot on an iPhone: four field reports land on `died-in-phase` / `boot:ddl-meta` — PGlite's first `exec()`, where Postgres-in-WASM actually starts and demands a **128 MB contiguous heap** declared by `pglite.wasm` (not a setting; a smaller `initialMemory` fails with `LinkError`). Four shipped fixes (#2420/#2422/#2427/#2431 — bundler release, `:memory:` dataDir, OPFS GC, un-eager LikeC4) each freed single-digit MB against that demand and **none moved the failure**. Measured against `e1b342a0`: eager JS is **12.80 MB**, of which Monaco is **9.56 MB (75%)** plus three worker realms, and `index-*.js` (2.57 MB) carries **the whole Loom compiler on the main thread** — chevrotain + Langium + grammar, pulled eagerly by `App.tsx → agent/* → src/tools → src/api` for one `runAgentDemo` symbol (a third resident copy, alongside `build.worker` and `ddd-server.worker`). On iOS the main thread and every worker share one WebContent memory budget, so moving work into a worker buys nothing — only deleting it does. The reframe: `isDesktop ? <DesktopShell/> : <MobileShell/>` picks a *layout* under one `App.tsx` that has already imported Monaco, the LSP and the compiler — **mobile pays the full desktop bill and renders a phone UI**. Mobile-light keeps source view/edit (plain textarea), generated-file view, problems (from `generate`, not the live LSP), the full generate→bundle→**boot** pipeline, Preview, the API explorer and (lazy) chat; it drops Monaco + `monaco-languageclient` + the LSP worker, Builder, Modeller and Requirements. Projected mobile eager JS **12.80 → ~1.2 MB** with three fewer worker realms. Implemented as lazy subgraphs behind a pre-render shell choice in `main.tsx` (**not** a second Vite entry — same shed, permanent two-app cost), with a **hard byte budget in `check-eager-chunks.mjs`** so the boundary is gated rather than conventional. **The owner constraint is fixed: the data layer / full app is a must** — UI-only mobile and desktop-only-runtime are both rejected. Success oracle is the phase marker moving past `boot:ddl-meta`; if it doesn't, 128 MB is more than the device will give a web page and the work redirects to a server-side runtime — slices 1–4 stand on their own merits either way.
**Slices 1–3 + 5 shipped — eager JS 12.80 MB → 1.63 MB (-87%).** (1) The agent modules are imported type-only and `await import(...)`ed at their call sites, so `src/tools` → `src/api` → the compiler left the entry chunk (2.57 → 1.01 MB). (2) Every Monaco consumer — `LoomEditor`, `FileViewer`, `JsonBodyEditor`, `LoomLspClient`, `monacoModelHost` — is reached only through `web/src/layout/lazy-panels.ts` or a dynamic import, and mobile renders plain counterparts instead (`PlainEditor` — textarea + scroll-synced gutter + Tab-indents, same `EditorHandle`/`__loomSetSource` seam; `PlainFileViewer`; `PlainJsonBody`), with the markdown/mermaid viewers extracted to `preview/doc-viewers.tsx` over an injected code panel so both surfaces share them. (3) Mobile constructs no language client at all and takes Problems from `generate` via `lsp/build-diagnostics.ts` (same compiler, same `file:line`, reported on Run). **The finding worth remembering:** slice 2's lazy boundaries alone moved eager JS 12.80 → 11.23 MB, i.e. nothing — the real cause was `\0vite/preload-helper`, the helper every `await import(...)` compiles to, which is a VIRTUAL module, matched no `manualChunks` rule, and got placed in the `monaco` vendor chunk; the entry's static import of the helper was a static import of 9.56 MB, `<link rel="modulepreload">` and all. Pinning the helper to its own chunk is what moved the number. Fourth instance of the chunk-grouping hazard, and the first reached through a module we don't own — a correct `await import()` at every call site is not evidence a chunk is lazy, only the emitted graph is. (5) `check-eager-chunks.mjs` now gates three ways: chunk names (`monaco` added), source SIGNATURES checked both for presence in `dist/` and absence from the eager graph (so it cannot rot into a permanent pass), and a hard **2.5 MB eager byte budget**. All mutation-proved. **Slice 4 (entry split in `main.tsx`) is deferred, not dropped** — it was scoped to remove Monaco from mobile's graph, which 1–3 achieved without it; what remains for it is 0.18 MB of eager `xyflow`. **Still `partial`, and deliberately: no claim that the iPhone boot is fixed until a phase marker moves past `boot:ddl-meta`.** Four "this should help" fixes already failed that test; an 87% cut in resident JS is the fifth until a device says otherwise.
Design: [M-T8.15-mobile-light-design](missions/M-T8.15-mobile-light-design.md). Sources: [playground.md](../playground.md), M-T8.14 (the phase-marker instrument this depends on).

## M-T8.16 — Playground: pipeline strip, one vocabulary, honest dock — `done` (PR #2757) · **M** · P1
The Generate → Bundle → Boot contract is invisible: three controls in three places, disabled with no reason, a different verb on mobile, and dock status carried by 7 px colour dots with no text. One `PipelineStrip` (Validate · Generate · Bundle · Boot, each with state + count, click runs up to it, hover explains a blocker; mobile keeps **Run** with four dots), a `vocabulary.ts` every user-visible string imports from (ratcheted by a literal-grep test), the dock as a real tablist with count badges, a mobile **More** sheet replacing the two-row tab bar, header overflow menu, `forceColorScheme`. Keeps every `btn-*` / `devtools-tab-*` / `mobile-tab-*` test id. Wave 1 of the program; file-disjoint from M-T8.17. Closes audit H1, M1, M3, M4, M7, M11, M12, M16.
Design: [M-T8.16-playground-ux-program](missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-ux-review-2026-09](../audits/playground-ux-review-2026-09.md), [playground-landscape-research-2026-09](../audits/playground-landscape-research-2026-09.md) §4 #14, #25.

## M-T8.17 — Playground: confirm / undo layer + the silent no-op drain — `done` (#2758, stacked on the program base) · **M** · P1
The "lost work" class: seven `window.confirm`/`prompt` sites, unconfirmed declaration deletes on the model canvas (while a cosmetic layout reset *does* confirm), no Undo from any visual surface (the Monaco undo stack is correct but unreachable; mobile's `PlainEditor` wipes even the native one), canvas edits discarded on page switch / live re-seed with no dirty flag, and eight `if (next != null) apply(next)` mutations that no-op silently with a refusal line that names nothing. One `ConfirmAction` (inline + modal, type-to-confirm for workspace delete), Undo/Redo in every pane bound to the editor stack, dirty guards, `applyOrRefuse` everywhere with a refusal that names construct + reason + shows the candidate. Wave 1. Closes H8, H9, H10, H11 (M15's keyboard rows stay with M-T8.21's icon-button pass).
Shipped 2026-09-03: `web/src/util/confirm{-state.ts,.tsx}` (catalog + machine + Inline/Modal/Host), `EditorHandle.undo/redo` + `builder/undo-redo.tsx`, craft-history dirty flag + form dirty guard, `harness.on(what)` named writers + `RefusalLine` with *Show candidate*, `add-palette-blockers.ts`. Gates: `test/playground/{confirm,undo-keys,palette-blockers}.test.ts` (the first ratchets `window.confirm|prompt|alert` out of `web/src`), `web/e2e/destructive-actions.spec.ts` (mutation-proved). Found on the way: the file tree's row ⋮ never opened (Tooltip-over-`Menu.Target`; fixed).
Design: [M-T8.16-playground-ux-program](missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-ux-review-2026-09](../audits/playground-ux-review-2026-09.md) §3.2.

## M-T8.18 — Playground: Problems as a teaching surface, `⌘K`, first run — `open` · **M** · P2
No onboarding, no help link, two keyboard handlers in the whole app, and a Problems list that shows a `loom.*` message with no code, no docs link and no fix even where `fix-hints.ts` has a provider. Problems rows gain code chip + docs anchor (`codeDocsUrl` beside the catalog, ratcheted) + **Fix** (the editor's own `resolvePatchEdits`) + *Ask the agent*, Errors/Warnings toggles, `F8`; a `⌘K` palette over every ctx action (`@mantine/spotlight`, owner-overridable) and a `?` shortcut sheet; a three-door first-run card (describe / example / write) and a `?` header menu; an examples pane by concept with read tracking; the four visual panes' parse-error state gets *Go to line N*. Wave 2 (needs M-T8.16's dock + header slot). Closes H5, H7, M14, M13 (dock).
Design: [M-T8.16-playground-ux-program](missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../audits/playground-landscape-research-2026-09.md) §2.2, §4 #18, #20; M-T8.5 (the fix-hint registry it reuses).

## M-T8.19 — Playground: agent loop as plan → receipt → checkpoint — `open` · **L** · P2
The builders' agent table stakes, done on the model instead of the transcript: chat as a centre tab with a Split beside Source (the source streaming next to the chat is the demo); a **plan step** that returns a model-node delta (from `loom_outline` of the candidate) the user approves before any `.ddd` is written; a per-turn **receipt** (the `.ddd` diff, the validator delta "2 errors → 0", the generated-file delta, tokens from the provider `usage`); every turn and every visual Apply is a labelled commit with a *Restore* on the message that restores **as a new commit** (undoable, says which point); loop detection after three same-diagnostic fix turns with an "I'm stuck" card; *Fix* code actions labelled free. Rides M-T8.3's transport and the `__loomAgentComplete` seam. Wave 3.
Design: [M-T8.16-playground-ux-program](missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../audits/playground-landscape-research-2026-09.md) §2.1, §2.4, §4 #3–#5, #12, #13; M-T8.3.

## M-T8.20 — Playground: `.loom/` as views, output diff, source ↔ output correspondence — `open` · **L** · P2
The `.loom/` bundle is generated and then shown as a file tree; the sourcemap that maps every generated line to its `.ddd` span exists for `ddd trace` and is unused by the UI. Explorer switcher grows Diagrams (mermaid live on generate), API (OpenAPI/AsyncAPI), Migrations, Traceability as rendered views; changed-since-last-generate markers and a per-commit generated-output diff; the build worker always records the sourcemap so hovering a declaration highlights the files/lines it produced and hovering generated code flashes the span (godbolt's colour mapping — the category's most memorable feature, across five backends and six frontends); a preview *Select* mode resolving a `data-testid` to its page/primitive. Depends on M-T8.2. Wave 3.
Design: [M-T8.16-playground-ux-program](missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../audits/playground-landscape-research-2026-09.md) §2.2, §2.3, §4 #15–#17, #23, #24; M-T8.2; [loom-artifacts](../loom-artifacts.md).

## M-T8.21 — Playground: visual-pane empty states + the scaffold gradient — `open` · **S** · P2
The Builder tab is empty for the default example because its pages come from `scaffold`, and the copy tells the user to write a `ui { page }` block — the customisation gradient hidden at the one place it should be visible. Scaffold-aware page rows with a badge and an **Unfold** action (the LSP unfold-macro code action through the editor handle); drill-level empty states with a *Back to {parent}* button; the unicode glyph buttons (✎ × ↑ ↓ ƒ ⋯ ⇄) replaced by a labelled inline-SVG set per `ExpressionEditor`'s convention; keyboard-operable requirement rows; middle-ellipsis paths; a detail-level switch in the Model pane. Wave 2 (reuses M-T8.17's primitives and M-T8.18's go-to-line). Closes H6, H7 (panes), M13, M17.
Design: [M-T8.16-playground-ux-program](missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-ux-review-2026-09](../audits/playground-ux-review-2026-09.md), [customization-gradient](../customization-gradient.md), [playground-landscape-research-2026-09](../audits/playground-landscape-research-2026-09.md) §6.

## M-T8.22 — Playground: runtime & evolution surfaces — `done` (PR #2762, stacked on the program base) · **M** · P2
The model-driven family's best patterns over data the playground already holds: the Migrations tab rendered as the aggregate diagram tinted by the diff (added / changed / untouched) beside the SQL with destructive ops as a named gate (Prisma Studio's view over `MigrationsIR`); verdicts in sentence case with a legend; a read-only *Tables* view and a *Users* strip in Runtime; requests from the preview counted on the operation nodes in the Model pane with a 404s list (Darklang's traces); every loading state with a failure branch and every raw error with one line of interpretation and a link to the right Output stream. Extends M-T8.11. Wave 2. Closes M8, M9, M10, M18, M19.
Design: [M-T8.16-playground-ux-program](missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../audits/playground-landscape-research-2026-09.md) §2.3, §4 #22, #26; M-T8.11.
Shipped 2026-09-03 (five slices): **Migrations** — "Compare with" / "Last save", the auto-run's loading line, a Refresh that keeps its label, and no copy that asks for a click the panel already made (M8); the schema drawn as a table diagram tinted by the diff (added / changed / removed / untouched — `web/src/layout/migration-tint.ts` over a worker DTO that now carries `tables` + a per-step `table`) beside the SQL, with a destructive migration rendered as the gate `ddd generate system` raises (`--allow-destructive` + the data it would drop). **Verdicts** — sentence case + one legend (`VERDICT_LABEL` / `VERDICT_LEGEND` in `vocabulary.ts`, shared with M-T8.21); the discovery error gets an interpretation line with the raw text folded (M9, M19). **Runtime tab** — read-only *Tables* (`information_schema` → first 50 rows through `runQuery`) with a *Users* strip (the generated dev stub's built-in identity + the Auth-tab override), and one interpretation line + a See Output → Runtime logs link above every boot / response / dispatch error (M19). **Requests** — `web/src/backend/route-match.ts` matches the runtime log's `request_end` lines to OpenAPI operations; per-operation counts + a 404s list in the Runtime tab; `ctx.requestTraces` exposed for M-T8.20's Model-node counts. **Loading states** — failure branches with Retry on History's commit changes, the baseline list and the pack list; per-suite progress + Cancel on test discovery; the Diagnostics stream in sentences with the ring key kept as a chip (M10, M18). Gates: `test/playground/{route-match,migration-tint,diag-humanize,runtime-views}.test.ts`, `web/e2e/runtime-surfaces.spec.ts` (no-network, mutation-proved), a heavy-lane addition to `web/e2e/runtime.spec.ts` (Tables lists `products`; GET /products counted under Requests — not run in the authoring sandbox). Closes M8, M9, M10, M18, M19; the Migrations-tab half of M-T8.11's surface now lives here.

## M-T8.23 — Playground: targets drawer, read-only links, export — `open` · **M** · P3
Reach and polish after the loops are right: a Targets drawer that rewrites the deployable clauses (platform / framework / design pack) through `applyPatches` — the compiler playground's version selector applied to the stack; `#view=1` read-only links and an `embed` flag, with the share dialog saying what a link carries; a compose README in the ZIP; the 108-site `dark.N` → semantic-token migration so `forceColorScheme` can go; the audit's L1–L6. Short links and GitHub push are recorded as **deliberately out of scope** (static site, no token story). Wave 3.
Design: [M-T8.16-playground-ux-program](missions/M-T8.16-playground-ux-program.md) §3. Sources: [playground-landscape-research-2026-09](../audits/playground-landscape-research-2026-09.md) §4 #19, #21, #29; [api-toolkit](../api-toolkit.md) (`applyPatches`).
