<div class="flex flex-col gap-2">
          <label class="loom-label" for="{{testId}}">{{label}}</label>
          <input id="{{testId}}" class="loom-input" data-testid="{{testId}}" placeholder="(arrays not yet supported in forms)" disabled />
        </div>