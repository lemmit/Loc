// Auto-generated.  The app shell — a standalone component (selector
// app-root) rendering the Material toolbar + sidenav chrome around the
// router outlet.  Nav labels/links are baked at generation time.
import { Component{{#if navUsesSession}}, inject{{/if}} } from "@angular/core";
import { MatIconModule } from "@angular/material/icon";
import { MatListModule } from "@angular/material/list";
import { MatSidenavModule } from "@angular/material/sidenav";
import { MatToolbarModule } from "@angular/material/toolbar";
import { RouterLink,{{#if hasNav}} RouterLinkActive,{{/if}} RouterOutlet } from "@angular/router";
{{#if authUi}}import { AuthGateComponent } from "./auth/auth-gate.component";
{{/if}}{{#if navUsesSession}}import { SessionService } from "./auth/session.service";
{{/if}}
@Component({
  selector: "app-root",
  imports: [
    RouterOutlet,
    RouterLink,
{{#if hasNav}}    RouterLinkActive,
{{/if}}    MatToolbarModule,
    MatSidenavModule,
    MatListModule,
    MatIconModule,
{{#if authUi}}    AuthGateComponent,
{{/if}}  ],
  template: `
    <a href="#main-content" class="loom-skip-link">Skip to content</a>
    <mat-toolbar color="primary">
      <a routerLink="/" class="loom-brand">{{systemNameHuman}}</a>
    </mat-toolbar>
    <mat-sidenav-container class="loom-shell">
      <mat-sidenav mode="side" opened class="loom-sidenav" role="navigation" aria-label="Primary navigation">
        <mat-nav-list data-testid="nav-sidebar">
{{#each navSections}}          <h3 matSubheader>{{label}}</h3>
{{#each entries}}{{#if external}}          <a mat-list-item href="{{href}}" target="_blank" rel="noopener" data-testid="{{testId}}">{{label}}</a>
{{else}}{{#if requiresJs}}          @if ({{{requiresJs}}}) { <a mat-list-item routerLink="{{to}}" routerLinkActive="loom-active" data-testid="{{testId}}">{{label}}</a> }
{{else}}          <a mat-list-item routerLink="{{to}}" routerLinkActive="loom-active" data-testid="{{testId}}">{{label}}</a>
{{/if}}{{/if}}{{/each}}{{/each}}        </mat-nav-list>
      </mat-sidenav>
      <mat-sidenav-content class="loom-content" id="main-content" role="main">
{{#if authUi}}        <app-auth-gate>
          <router-outlet />
        </app-auth-gate>
{{else}}        <router-outlet />
{{/if}}      </mat-sidenav-content>
    </mat-sidenav-container>
  `,
  styles: [
    `
    .loom-brand { color: inherit; text-decoration: none; font-weight: 600; }
    .loom-shell { height: calc(100vh - 64px); }
    .loom-sidenav { width: 240px; }
    .loom-content { padding: 16px; }
    .loom-active { background: rgba(0, 0, 0, 0.06); }
    .loom-skip-link { position: absolute; left: -9999px; top: 0; z-index: 3000; padding: 8px 16px; background: Canvas; color: CanvasText; border: 1px solid; border-radius: 4px; }
    .loom-skip-link:focus { left: 8px; top: 8px; }
  `,
  ],
})
{{#if navUsesSession}}export class AppComponent {
  readonly session = inject(SessionService);
  get currentUser(): Record<string, unknown> { return this.session.user() ?? {}; }
}{{else}}export class AppComponent {}{{/if}}
