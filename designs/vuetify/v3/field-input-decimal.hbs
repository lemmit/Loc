<v-text-field label="{{label}}" data-testid="{{testId}}" type="number" step="0.01" :model-value="form.{{path}}" @update:model-value="(v) => form.{{path}} = Number(v) || 0" :error-messages="{{errorExpr}}" />

