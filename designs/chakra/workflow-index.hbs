// Auto-generated.
import { Link } from "react-router-dom";
import {
  Breadcrumb, BreadcrumbItem, BreadcrumbLink,
  VStack, HStack, Text, Heading, Card, CardBody,
  Button, SimpleGrid,
} from "@chakra-ui/react";

export default function WorkflowsIndex() {
  return (
    <VStack data-testid="workflows-index" gap={4} alignItems="stretch">
      <Breadcrumb data-testid="workflows-index-breadcrumbs">
        <BreadcrumbItem><BreadcrumbLink as={Link} to="/">Home</BreadcrumbLink></BreadcrumbItem>
        <BreadcrumbItem isCurrentPage><BreadcrumbLink>Workflows</BreadcrumbLink></BreadcrumbItem>
      </Breadcrumb>
      <VStack gap={0} alignItems="flex-start">
        <Heading size="md">Workflows</Heading>
        <Text color="gray.500">System-level orchestrations. Pick one to run.</Text>
      </VStack>
      <SimpleGrid columns=\{{ base: 1, sm: 2 }} spacing={4}>
{{#each cards}}      <Card variant="outline" data-testid="workflow-card-{{slug}}">
        <CardBody>
          <VStack gap={2} alignItems="stretch">
            <HStack justifyContent="space-between" alignItems="center">
              <HStack gap={2} alignItems="center">
                <Text fontSize="sm">⚡</Text>
                <Heading size="sm">{{humanWorkflow}}</Heading>
              </HStack>
              <Button as={Link} to="/workflows/{{slug}}" colorScheme="brand" size="sm" data-testid="workflow-{{slug}}-run">Run →</Button>
            </HStack>
{{#if hasParams}}{{#each params}}            <Text fontSize="sm" color="gray.500" data-testid="workflow-{{../slug}}-param-{{name}}"><strong>{{humanName}}</strong>: {{expr typeLabelJson}}</Text>
{{/each}}{{else}}            <Text fontSize="sm" color="gray.500">No parameters.</Text>
{{/if}}          </VStack>
        </CardBody>
      </Card>
{{/each}}      </SimpleGrid>
    </VStack>
  );
}
