  const create = useCreate{{aggregateName}}();
{{#each idTargets}}  const {{this.hookVar}} = useAll{{this.namePlural}}();
{{/each}}{{#if hasDefaultOnSubmit}}  const toast = useToast();
{{/if}}  const {{#if useController}}{ register, handleSubmit, control, formState: { errors } }{{else}}{ register, handleSubmit, formState: { errors } }{{/if}} = useForm<Create{{aggregateName}}Request>({
    resolver: zodResolver(Create{{aggregateName}}Request),
    defaultValues: {{{defaultValuesTs}}},
  });
