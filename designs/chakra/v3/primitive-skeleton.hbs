{{#if isMulti}}<VStack gap={2}{{{testidAttr}}}>
{ Array.from({ length: {{count}} }).map((_, i) => (
<Skeleton key={i} height={ {{height}} } />
)) }
</VStack>{{else}}<Skeleton height={ {{height}} }{{{testidAttr}}} />{{/if}}