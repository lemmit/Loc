// Auto-generated.
import { VStack, HStack, Text, Heading, Link as ChakraLink, Card, CardBody, SimpleGrid } from "@chakra-ui/react";
import { Link } from "react-router-dom";

export default function Home() {
  return (
    <VStack data-testid="home" gap={4} alignItems="stretch">
      <VStack gap={1} alignItems="flex-start">
        <Text fontSize="xs" color="gray.500" textTransform="uppercase" letterSpacing="wide">{{systemNameHuman}}</Text>
        <Heading size="md">Welcome</Heading>
        <Text color="gray.500">
          Pick a section from the sidebar to start, or jump straight in below.
        </Text>
      </VStack>
      <SimpleGrid columns=\{{ base: 1, sm: 2, md: 3 }} spacing={4}>
        <Card variant="outline">
          <CardBody>
            <VStack gap={1} alignItems="flex-start">
              <Text fontWeight={600}>{{aggregateCount}} aggregate{{#unless (eq aggregateCount 1)}}s{{/unless}}</Text>
              <Text fontSize="sm" color="gray.500">Manage records of each kind from the sidebar.</Text>
{{#if firstAggregateSlug}}              <ChakraLink as={Link} to="/{{firstAggregateSlug}}" data-testid="home-aggregates-link" color="brand.600">Browse the sidebar →</ChakraLink>
{{/if}}            </VStack>
          </CardBody>
        </Card>
{{#if workflowCount}}        <Card variant="outline">
          <CardBody>
            <VStack gap={1} alignItems="flex-start">
              <Text fontWeight={600}>{{workflowCount}} workflow{{#unless (eq workflowCount 1)}}s{{/unless}}</Text>
              <Text fontSize="sm" color="gray.500">System-level orchestrations you can run from a form.</Text>
              <ChakraLink as={Link} to="/workflows" data-testid="home-workflows-link" fontSize="sm" color="brand.600">Open workflows →</ChakraLink>
            </VStack>
          </CardBody>
        </Card>
{{/if}}{{#if viewCount}}        <Card variant="outline">
          <CardBody>
            <VStack gap={1} alignItems="flex-start">
              <Text fontWeight={600}>{{viewCount}} view{{#unless (eq viewCount 1)}}s{{/unless}}</Text>
              <Text fontSize="sm" color="gray.500">Saved queries — open one to inspect rows.</Text>
              <ChakraLink as={Link} to="/views" data-testid="home-views-link" fontSize="sm" color="brand.600">Open views →</ChakraLink>
            </VStack>
          </CardBody>
        </Card>
{{/if}}      </SimpleGrid>
    </VStack>
  );
}
