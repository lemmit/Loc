{{#if hasBind}}<FormControl{{#if hasError}} isInvalid={Boolean( {{{error}}} )}{{/if}}{{{testidAttr}}}><FormLabel>{{{labelAttr}}}</FormLabel><Input type="file" onChange={async (e) => { const f = e.target.files?.[0]; if (!f) { {{setter}}(null); return; } const fd = new FormData(); fd.append("file", f); {{setter}}(await api.upload("/files", fd)); }} />{{#if hasError}}<FormErrorMessage>{ {{{error}}} }</FormErrorMessage>{{/if}}</FormControl>{{else}}<FormControl{{{testidAttr}}}><FormLabel>{{{labelAttr}}}</FormLabel><Input type="file" /></FormControl>{{/if}}
