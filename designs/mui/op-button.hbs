<Button variant="{{#if (eq variant "filled")}}contained{{else}}outlined{{/if}}"{{#if icon}} startIcon={<{{muiIcon icon}}Icon />}{{/if}} onClick={() => setActiveOp("{{name}}")} data-testid="{{testId}}">{{humanName}}</Button>