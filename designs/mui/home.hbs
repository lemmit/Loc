// Auto-generated.
import { Stack, Typography, Link as MuiLink, Card, CardContent, Grid } from "@mui/material";
import { Link } from "react-router-dom";

export default function Home() {
  return (
    <Stack data-testid="home" gap={2}>
      <Stack gap={0.5}>
        <Typography variant="overline" color="text.secondary">{{systemNameHuman}}</Typography>
        <Typography variant="h4">Welcome</Typography>
        <Typography color="text.secondary">
          Pick a section from the sidebar to start, or jump straight in below.
        </Typography>
      </Stack>
      <Grid container spacing={2}>
        <Grid item xs={12} sm={6} md={4}>
          <Card>
            <CardContent>
              <Stack gap={0.5}>
                <Typography fontWeight={600}>{{aggregateCount}} aggregate{{#unless (eq aggregateCount 1)}}s{{/unless}}</Typography>
                <Typography variant="body2" color="text.secondary">Manage records of each kind from the sidebar.</Typography>
{{#if firstAggregateSlug}}                <MuiLink component={Link} to="/{{firstAggregateSlug}}" data-testid="home-aggregates-link">Browse the sidebar →</MuiLink>
{{/if}}              </Stack>
            </CardContent>
          </Card>
        </Grid>
{{#if workflowCount}}        <Grid item xs={12} sm={6} md={4}>
          <Card>
            <CardContent>
              <Stack gap={0.5}>
                <Typography fontWeight={600}>{{workflowCount}} workflow{{#unless (eq workflowCount 1)}}s{{/unless}}</Typography>
                <Typography variant="body2" color="text.secondary">System-level orchestrations you can run from a form.</Typography>
                <MuiLink component={Link} to="/workflows" data-testid="home-workflows-link" variant="body2">Open workflows →</MuiLink>
              </Stack>
            </CardContent>
          </Card>
        </Grid>
{{/if}}{{#if viewCount}}        <Grid item xs={12} sm={6} md={4}>
          <Card>
            <CardContent>
              <Stack gap={0.5}>
                <Typography fontWeight={600}>{{viewCount}} view{{#unless (eq viewCount 1)}}s{{/unless}}</Typography>
                <Typography variant="body2" color="text.secondary">Saved queries — open one to inspect rows.</Typography>
                <MuiLink component={Link} to="/views" data-testid="home-views-link" variant="body2">Open views →</MuiLink>
              </Stack>
            </CardContent>
          </Card>
        </Grid>
{{/if}}      </Grid>
    </Stack>
  );
}
