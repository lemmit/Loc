// Auto-generated.
import { Link } from "react-router-dom";
import {
  Link as MuiLink, Breadcrumbs, Stack, Typography, Card, CardContent,
  Button, Grid,
} from "@mui/material";
import BoltIcon from "@mui/icons-material/Bolt";

export default function WorkflowsIndex() {
  return (
    <Stack data-testid="workflows-index" gap={2}>
      <Breadcrumbs data-testid="workflows-index-breadcrumbs">
        <MuiLink component={Link} to="/">Home</MuiLink>
        <Typography>Workflows</Typography>
      </Breadcrumbs>
      <Stack gap={0.5}>
        <Typography variant="h5">Workflows</Typography>
        <Typography color="text.secondary">System-level orchestrations. Pick one to run.</Typography>
      </Stack>
      <Grid container spacing={2}>
{{#each cards}}      <Grid item xs={12} sm={6}>
        <Card data-testid="workflow-card-{{slug}}">
          <CardContent>
            <Stack gap={1}>
              <Stack direction="row" justifyContent="space-between" alignItems="center">
                <Stack direction="row" gap={1} alignItems="center">
                  <BoltIcon fontSize="small" color="primary" />
                  <Typography variant="h6">{{humanWorkflow}}</Typography>
                </Stack>
                <Button component={Link} to="/workflows/{{slug}}" variant="contained" size="small" data-testid="workflow-{{slug}}-run">Run →</Button>
              </Stack>
{{#if hasParams}}{{#each params}}              <Typography variant="body2" color="text.secondary" data-testid="workflow-{{../slug}}-param-{{name}}"><strong>{{humanName}}</strong>: {{expr typeLabelJson}}</Typography>
{{/each}}{{else}}              <Typography variant="body2" color="text.secondary">No parameters.</Typography>
{{/if}}            </Stack>
          </CardContent>
        </Card>
      </Grid>
{{/each}}      </Grid>
    </Stack>
  );
}
