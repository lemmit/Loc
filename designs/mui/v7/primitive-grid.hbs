{{#if hasChildren}}<Grid container spacing={2}{{{testidAttr}}}{{{styleAttr}}}>
{{colIndent}}{{#each children}}{{#if ../hasResponsiveCols}}<Grid size=\{{ xs: 12, sm: {{../spanTablet}}, lg: {{../spanDesktop}} }}>{{else}}<Grid size=\{{ xs: 12, sm: "auto" }}>{{/if}}
{{../childIndent}}{{{this}}}
{{../colIndent}}</Grid>{{#unless @last}}
{{../colIndent}}{{/unless}}{{/each}}
{{closeIndent}}</Grid>{{else}}<Grid container{{{testidAttr}}}{{{styleAttr}}} />{{/if}}