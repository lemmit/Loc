{{#if hasBind}}<TextField type="password" label={{{labelAttr}}} value={{expr bind}} onChange={(e) => {{setter}}(e.target.value)}{{{testidAttr}}} />{{else}}<TextField type="password" label={{{labelAttr}}}{{{testidAttr}}} />{{/if}}