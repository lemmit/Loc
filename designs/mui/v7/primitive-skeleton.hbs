{{#if isMulti}}<Stack gap={1}{{{testidAttr}}} aria-hidden="true">
{ Array.from({ length: {{count}} }).map((_, i) => (
<Skeleton key={i} height={ {{height}} } />
)) }
</Stack>{{else}}<Skeleton height={ {{height}} }{{{testidAttr}}} aria-hidden="true" />{{/if}}