// Auto-generated.
import { Link } from "react-router-dom";
import {
  Link as MuiLink, Breadcrumbs, Stack, Typography, Card, CardContent,
  Button, Grid,
} from "@mui/material";
import ViewListIcon from "@mui/icons-material/ViewList";

export default function ViewsIndex() {
  return (
    <Stack data-testid="views-index" gap={2}>
      <Breadcrumbs data-testid="views-index-breadcrumbs">
        <MuiLink component={Link} to="/">Home</MuiLink>
        <Typography>Views</Typography>
      </Breadcrumbs>
      <Stack gap={0.5}>
        <Typography variant="h5">Views</Typography>
        <Typography color="text.secondary">Saved queries. Pick one to inspect.</Typography>
      </Stack>
      <Grid container spacing={2}>
{{#each cards}}      <Grid item xs={12} sm={6}>
        <Card data-testid="view-card-{{slug}}">
          <CardContent>
            <Stack gap={1}>
              <Stack direction="row" justifyContent="space-between" alignItems="center">
                <Stack direction="row" gap={1} alignItems="center">
                  <ViewListIcon fontSize="small" color="primary" />
                  <Typography variant="h6">{{humanView}}</Typography>
                </Stack>
                <Button component={Link} to="/views/{{slug}}" variant="outlined" size="small" data-testid="view-{{slug}}-open">Open →</Button>
              </Stack>
              <Typography variant="body2" color="text.secondary">{{shapeLine}}</Typography>
            </Stack>
          </CardContent>
        </Card>
      </Grid>
{{/each}}      </Grid>
    </Stack>
  );
}
