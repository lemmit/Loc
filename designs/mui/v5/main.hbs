// Auto-generated.
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "{{routerPackage}}";
import { ThemeProvider } from "@mui/material/styles";
import CssBaseline from "@mui/material/CssBaseline";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { SnackbarProvider } from "notistack";
import App from "./App";
{{#if authUi}}import { AuthGate } from "./auth/AuthGate";
{{/if}}import { ErrorBoundary } from "./ErrorBoundary";
import { theme } from "./theme";

const queryClient = new QueryClient({
  defaultOptions: { queries: { staleTime: 5_000, retry: 1 } },
});

const basename =
  (typeof window !== "undefined"
    ? (window as { __LOOM_BASENAME__?: string }).__LOOM_BASENAME__
    : undefined) ?? {{#if basename}}{{json basename}}{{else}}undefined{{/if}};

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ErrorBoundary>
    <QueryClientProvider client={queryClient}>
      <ThemeProvider theme={theme}>
        <CssBaseline />
        <SnackbarProvider maxSnack={3} anchorOrigin=\{{ vertical: "top", horizontal: "right" }}>
          <BrowserRouter basename={basename}>
{{#if authUi}}            <AuthGate>
              <App />
            </AuthGate>
{{else}}            <App />
{{/if}}          </BrowserRouter>
        </SnackbarProvider>
      </ThemeProvider>
    </QueryClientProvider>
    </ErrorBoundary>
  </React.StrictMode>,
);
