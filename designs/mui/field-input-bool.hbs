<Controller
          control={control}
          name="{{path}}"
          render={({ field }) => (
            <FormControlLabel control={<Switch checked={!!field.value} onChange={(e) => field.onChange(e.target.checked)} data-testid="{{testId}}" />} label="{{label}}" />
          )}
        />