{{#if hasBind}}<div className="flex flex-col gap-2"><Label>{{{labelText}}}</Label><Input type="password" value={{expr bind}} onChange={(e) => {{setter}}(e.currentTarget.value)}{{{testidAttr}}} />{{#if hasError}}{( {{{error}}} ) ? <p className="text-sm text-destructive">{ {{{error}}} }</p> : null}{{/if}}</div>{{else}}<div className="flex flex-col gap-2"><Label>{{{labelText}}}</Label><Input type="password"{{{testidAttr}}} />{{#if hasError}}{( {{{error}}} ) ? <p className="text-sm text-destructive">{ {{{error}}} }</p> : null}{{/if}}</div>{{/if}}