{{#if hasBind}}<div className="flex flex-col gap-2"><Label>{{{labelText}}}</Label><Input type="password" value={{expr bind}} onChange={(e) => {{setter}}(e.currentTarget.value)} /></div>{{else}}<div className="flex flex-col gap-2"><Label>{{{labelText}}}</Label><Input type="password" /></div>{{/if}}