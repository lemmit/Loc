// Auto-generated.
import { Link, useNavigate } from "react-router-dom";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { Alert, AlertDescription, AlertTitle } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { AlertCircle, Plus } from "lucide-react";
import { {{hookName}} } from "{{{hookImportPath}}}";
import { IdValue, DateTimeValue, BoolValue, NumberValue, EmptyValue } from "@/lib/format";

export default function {{aggregateName}}List() {
  const navigate = useNavigate();
  const q = {{hookName}}();
  const count = q.data?.length ?? 0;
  return (
    <div data-testid="{{slug}}-list" className="flex flex-col gap-4">
      <nav data-testid="{{slug}}-list-breadcrumbs" className="flex items-center gap-2 text-sm text-muted-foreground">
        {{#each breadcrumbs}}{{#if to}}<Link to="{{{to}}}" className="hover:text-foreground">{{label}}</Link>{{else}}<span>{{label}}</span>{{/if}}{{#unless @last}} <span>/</span> {{/unless}}{{/each}}
      </nav>
      <div className="flex items-end justify-between">
        <div className="flex flex-col gap-0.5">
          <h2 className="text-2xl font-semibold tracking-tight">{{humanPlural}}</h2>
          <p className="text-sm text-muted-foreground">{q.isLoading ? "Loading…" : count === 1 ? "1 record" : count + " records"}</p>
        </div>
        <Button onClick={() => navigate("/{{slug}}/new")} data-testid="{{slug}}-list-create"><Plus className="mr-2 h-4 w-4" />New {{humanSingularLower}}</Button>
      </div>
      {q.isLoading && (
        <Card className="p-4">
          <div className="flex flex-col gap-2">
            {Array.from({ length: 5 }).map((_, i) => (
              <Skeleton key={i} className="h-7" />
            ))}
          </div>
        </Card>
      )}
      {q.isError && (
        <Alert variant="destructive">
          <AlertCircle className="h-4 w-4" />
          <AlertTitle>Couldn't load {{humanPluralLower}}</AlertTitle>
          <AlertDescription>{(q.error as Error).message}</AlertDescription>
        </Alert>
      )}
      {q.data && q.data.length === 0 && (
        <Card className="p-12" data-testid="{{slug}}-list-empty">
          <div className="flex flex-col items-center gap-2">
            <p className="text-sm text-muted-foreground">No {{humanPluralLower}} yet.</p>
            <Button variant="link" onClick={() => navigate("/{{slug}}/new")}>
              Create your first {{humanSingularLower}}
            </Button>
          </div>
        </Card>
      )}
      {q.data && q.data.length > 0 && (
        <Card>
          <Table>
            <TableHeader>
              <TableRow>
                {{#each columns}}<TableHead>{{title}}</TableHead>
                {{/each}}
              </TableRow>
            </TableHeader>
            <TableBody>
              {q.data.map((row) => (
                <TableRow key={row.id} data-testid={`{{slug}}-row-${row.id}`} className="cursor-pointer" onClick={() => navigate(`/{{slug}}/${row.id}`)}>
                  {{#each columns}}{{> (concat "cell-" kind)}}
                  {{/each}}
                </TableRow>
              ))}
            </TableBody>
          </Table>
        </Card>
      )}
    </div>
  );
}
