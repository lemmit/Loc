// Auto-generated.
import { Link } from "react-router-dom";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Zap } from "lucide-react";

export default function WorkflowsIndex() {
  return (
    <div data-testid="workflows-index" className="flex flex-col gap-6">
      <nav data-testid="workflows-index-breadcrumbs" className="flex items-center gap-2 text-sm text-muted-foreground">
        <Link to="/" className="hover:text-foreground">Home</Link>
        <span>/</span>
        <span>Workflows</span>
      </nav>
      <div className="flex flex-col gap-1">
        <h2 className="text-2xl font-semibold tracking-tight">Workflows</h2>
        <p className="text-muted-foreground">System-level orchestrations.  Pick one to run.</p>
      </div>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
{{#each cards}}        <Card data-testid="workflow-card-{{slug}}">
          <CardContent className="flex flex-col gap-2 p-6">
            <div className="flex items-center justify-between">
              <div className="flex items-center gap-2">
                <Zap className="h-4 w-4 text-primary" />
                <h4 className="text-base font-semibold">{{humanWorkflow}}</h4>
              </div>
              <Button asChild variant="default" size="sm">
                <Link to="/workflows/{{slug}}" data-testid="workflow-{{slug}}-run">Run →</Link>
              </Button>
            </div>
{{#if hasParams}}{{#each params}}            <p className="text-sm text-muted-foreground" data-testid="workflow-{{../slug}}-param-{{name}}"><strong className="font-semibold text-foreground">{{humanName}}</strong>: {{expr typeLabelJson}}</p>
{{/each}}{{else}}            <p className="text-sm text-muted-foreground">No parameters.</p>
{{/if}}          </CardContent>
        </Card>
{{/each}}      </div>
    </div>
  );
}
