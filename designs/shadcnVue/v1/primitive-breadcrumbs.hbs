{{#if hasChildren}}<nav class="flex items-center gap-2 text-sm text-muted-foreground"{{{testidAttr}}}>
{{indent}}{{{childrenBlock}}}
{{closeIndent}}</nav>{{else}}<nav class="flex items-center gap-2 text-sm text-muted-foreground"{{{testidAttr}}}></nav>{{/if}}
