{{#if isMulti}}<Stack gap="xs"{{{testidAttr}}} aria-hidden="true">
{ Array.from({ length: {{count}} }).map((_, i) => (
<Skeleton key={i} height={ {{height}} } radius="sm" />
)) }
</Stack>{{else}}<Skeleton height={ {{height}} } radius="sm"{{{testidAttr}}} aria-hidden="true" />{{/if}}