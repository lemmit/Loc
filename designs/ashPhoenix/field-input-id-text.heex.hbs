<%!-- field-input-id-text: text input for an Id field when the target is unknown --%>
<.input field={@form[:"{{path}}"]} type="text" label="{{label}}" placeholder={{placeholderJson}} data-testid="{{testId}}" />
