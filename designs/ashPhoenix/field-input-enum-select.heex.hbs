<%!-- field-input-enum-select: select for an enum field --%>
<.input field={@form[:"{{path}}"]} type="select"
        label="{{label}}"
        data-testid="{{testId}}"
        options={{{enumValuesJson}}}
        prompt="Select…" />
