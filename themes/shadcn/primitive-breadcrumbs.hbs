{{#if hasChildren}}<nav className="flex items-center gap-2 text-sm text-muted-foreground"{{{testidAttr}}}>
{{indent}}{{{childrenBlock}}}
{{closeIndent}}</nav>{{else}}<nav className="flex items-center gap-2 text-sm text-muted-foreground"{{{testidAttr}}} />{{/if}}