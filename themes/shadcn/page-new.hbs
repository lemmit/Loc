// Auto-generated.
import { Link, useNavigate } from "react-router-dom";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Switch } from "@/components/ui/switch";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from "@/components/ui/form";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { toast } from "sonner";
import { Create{{aggregateName}}Request, useCreate{{aggregateName}} } from "../../api/{{aggregateNameCamel}}";
{{#each idHookImportLines}}{{{this}}}
{{/each}}
export default function {{aggregateName}}New() {
  const navigate = useNavigate();
  const create = useCreate{{aggregateName}}();
{{#each idHookCalls}}{{{this}}}
{{/each}}  const form = useForm<Create{{aggregateName}}Request>({
    resolver: zodResolver(Create{{aggregateName}}Request),
    defaultValues: {{{defaultValuesTs}}},
  });
  return (
    <div data-testid="{{slug}}-new" className="flex max-w-2xl flex-col gap-4">
      <nav className="flex items-center gap-2 text-sm text-muted-foreground">
        <Link to="/" className="hover:text-foreground">Home</Link>
        <span>/</span>
        <Link to="/{{slug}}" className="hover:text-foreground">{{humanPlural}}</Link>
        <span>/</span>
        <span>New</span>
      </nav>
      <div className="flex flex-col gap-1">
        <p className="text-sm font-semibold uppercase tracking-wide text-muted-foreground">New {{humanAggLower}}</p>
        <h2 className="text-2xl font-semibold tracking-tight">Create {{humanAggLower}}</h2>
      </div>
      <Card>
        <CardContent className="p-6">
          <Form {...form}>
            <form
              onSubmit={form.handleSubmit(async (vals) => {
                try {
                  const out = await create.mutateAsync(vals);
                  toast.success("{{humanAgg}} created");
                  navigate(`/{{slug}}/${out.id}`);
                } catch (e) {
                  toast.error((e as Error).message);
                }
              })}
            >
              <div className="flex flex-col gap-4">
              {{#each fieldHtmls}}{{{this}}}
              {{/each}}
                <div className="flex justify-end gap-2 pt-2">
                  <Button type="button" variant="outline" onClick={() => navigate("/{{slug}}")}>Cancel</Button>
                  <Button type="submit" disabled={create.isPending} data-testid="{{slug}}-new-submit">Create</Button>
                </div>
              </div>
            </form>
          </Form>
        </CardContent>
      </Card>
    </div>
  );
}
