<td data-testid={{expr testIdExpr}}>{ {{{valueExpr}}} === null || {{{valueExpr}}} === undefined ? <span className="text-muted-foreground">—</span> : ({{{valueExpr}}} ? <span className="font-medium">Yes</span> : <span className="text-muted-foreground">No</span>)}</td>
