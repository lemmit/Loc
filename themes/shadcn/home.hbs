// Auto-generated.
import { Link } from "react-router-dom";
import { Card, CardContent } from "@/components/ui/card";

export default function Home() {
  return (
    <div data-testid="home" className="flex flex-col gap-6">
      <div className="flex flex-col gap-1">
        <p className="text-sm font-semibold uppercase tracking-wide text-muted-foreground">{{systemNameHuman}}</p>
        <h2 className="text-2xl font-semibold tracking-tight">Welcome</h2>
        <p className="text-muted-foreground">
          Pick a section from the sidebar to start, or jump straight in below.
        </p>
      </div>
      <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 md:grid-cols-3">
        <Card>
          <CardContent className="flex flex-col gap-1 p-6">
            <span className="font-semibold">{{aggregateCount}} aggregate{{#unless (eq aggregateCount 1)}}s{{/unless}}</span>
            <span className="text-sm text-muted-foreground">Manage records of each kind from the sidebar.</span>
{{#if firstAggregateSlug}}            <Link to="/{{firstAggregateSlug}}" data-testid="home-aggregates-link" className="text-sm text-primary hover:underline">Browse the sidebar →</Link>
{{/if}}          </CardContent>
        </Card>
{{#if workflowCount}}        <Card>
          <CardContent className="flex flex-col gap-1 p-6">
            <span className="font-semibold">{{workflowCount}} workflow{{#unless (eq workflowCount 1)}}s{{/unless}}</span>
            <span className="text-sm text-muted-foreground">System-level orchestrations you can run from a form.</span>
            <Link to="/workflows" data-testid="home-workflows-link" className="text-sm text-primary hover:underline">Open workflows →</Link>
          </CardContent>
        </Card>
{{/if}}{{#if viewCount}}        <Card>
          <CardContent className="flex flex-col gap-1 p-6">
            <span className="font-semibold">{{viewCount}} view{{#unless (eq viewCount 1)}}s{{/unless}}</span>
            <span className="text-sm text-muted-foreground">Saved queries — open one to inspect rows.</span>
            <Link to="/views" data-testid="home-views-link" className="text-sm text-primary hover:underline">Open views →</Link>
          </CardContent>
        </Card>
{{/if}}      </div>
    </div>
  );
}
