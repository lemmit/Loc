// Moved to `src/generator/_walker/api-hook-register.ts` (shared with
// the Svelte frontend).  Re-export shim so react-side import paths
// stay stable.
export * from "../../_walker/api-hook-register.js";
