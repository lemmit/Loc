// Moved to src/generator/_frontend/extern-functions.ts — the extern
// frontend-function builders are shared with the SvelteKit frontend
// (only the wire-DTO api-module root differs, threaded as a
// parameter).  Shim preserves the original import path.
export * from "../_frontend/extern-functions.js";
