// Moved to `src/generator/_frontend/views-module.ts` (shared with the
// Vue frontend — only the TanStack Query import specifier diverges).
// Re-export shim so react-side import paths stay stable.
export * from "../_frontend/views-module.js";
