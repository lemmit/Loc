// Moved to src/generator/_frontend/theme-preparer.ts — the theme VM
// preparer is framework-neutral (the elixir HEEx + vue generators
// consume it too).  Shim preserves the original import path.
export * from "../../../_frontend/theme-preparer.js";
