// Moved to `src/generator/_frontend/page-objects-builder.ts` (shared
// with the Svelte frontend — Playwright page objects key on testids,
// not on the rendering framework).  Re-export shim.
export * from "../_frontend/page-objects-builder.js";
