// M-FT.13 — generation defaults (stub; filled in by the implementation).
import { describe, it } from "vitest";

describe("generation defaults", () => {
  it.todo("lean compose: no observability services by default");
});
