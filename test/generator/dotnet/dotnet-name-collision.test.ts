// Placeholder — M-FT.10 claim stub.  Replaced by the real gate in this PR.
import { describe, it } from "vitest";

describe("dotnet member-name collisions", () => {
  it.todo("refuses an operation whose C# method name shadows a sibling type");
});
