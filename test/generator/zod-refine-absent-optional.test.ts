// M-FT.1 — wire `== null` means ABSENT, and every wire refine carries a
// human message.  Filled in by the implementation commit.
import { describe, it } from "vitest";

describe.todo("wire null-guard invariants admit an omitted optional field");
