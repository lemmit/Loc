// `ddd generate system` prunes what it no longer emits (M-FT.3 / finding G1).
//
// Skeleton — the gate lands with the implementation.
import { describe, it } from "vitest";

describe.todo("generate system — regeneration prunes stale output");
