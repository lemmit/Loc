// Placeholder — the optional-context-parameter silent-failure ratchet.
// Implemented in the following commit.
import { describe, it } from "vitest";
describe("optional-context-param sweep", () => {
  it.todo("ratchets context-shaped optional params in the shared cores");
});
