// M-FT.11 — parsing gate for the three grammar gaps the Fable field test hit:
//   F1  `key: string` as a field (hard keyword stolen by `Channel`'s `key:`)
//   F2  `if <cond> { … } else { … }` in an operation body
//   F3  `a ?? b` nullish coalescing, in operation AND page bodies
//
// Filled in by the implementation slices; this stub claims the file.
import { describe, it } from "vitest";

describe.todo("M-FT.11 grammar: key / if / ??");
